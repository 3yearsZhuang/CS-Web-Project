# 项目待办事项

> 本文件整合自全仓库各文档中所有**尚未完成**的任务与待办条目。
> 已完成/已归档的条目已迁移至 `项目演变历史.md`，此处不再保留。
>
> 图例：✅ 已完成 | 🔄 进行中/部分完成 | 无前缀 待处理

---

> 格式：`日期-项目编号：待办条目`
>
> 优先级含义：
> - **P0 急于落地**：阻塞交付正确性 / 影响真实用户功能 / 数据安全，须近期推进。
> - **P1 近期推进**：与主线强相关，本波次内收口。
> - **P2 可暂缓**：依赖规模/多实例触发或属远期新功能，当前单实例 <200 用户下不紧急。
> - **P3 观察/可选**：已知非致命、有意保留或可选增强。

---

# P0 急于落地（阻塞正确性 / 真实功能缺陷 / 数据安全）

> 本区 P0 项已全部完成（✅），已迁移归档至 `项目演变历史.md` 的 `2026-08-07 / 0.9.8 — P0 闭环` 段，此处不再保留。

# P1 近期推进（与主线强相关，本波次内收口）

> P1 已完成项（文档重叠核查、文档迁移待办3、部署拆分 tmux、camelCase 统一、静态扫描 CI）已迁移归档至 `项目演变历史.md` 的 `2026-08-07 / 0.9.8 — P1 闭环` 段。本区仅保留未完成项。

- 🔄2026-08-05-文档迁移-待办1：若需统一入口--后端加 docs 只读托管/API + 前端 viewer 加外部 Tab（方案②，优先级中）。状态：未做，依赖 docs 托管服务搭建，非当前阻塞项。
- 🔄2026-08-05-文档迁移-待办2：提炼跨仓库通用规范进前端 Devdocs-*（方案③，注意去重，优先级低）。状态：未做，优先级低。
- 🔄2026-07-31-Ops-ErrorBudget：2026-08 月度 Error Budget 记录--`FrontDoc-Ops.md` Part B 历史记录表框架已建，待 0.9.1 发布后首个完整月填数据（非现在可做）。
- 🔄2026-07-31-Ops-SLI / R18：可用性 SLI 外部探针未接入--`FrontDoc-Ops.md` Part B 已含 SLI/SLO 矩阵；外部探针降级为应用层日志统计，须接入外部探针（Sentry/GlitchTip 等，见 P2/L4），依赖外部服务，非当前阻塞项。
- ⚠️2026-07-31-R19-audit：**决策已变更（与旧待办冲突）**。旧计划"audit 失败不阻塞 CI、1.0 阶段仅警告"已被前端 `ci.yml` 的 **G1 发布门禁**取代：第 100-104 行注释明确"中危及以上漏洞**强制阻断**发布，不应再通过 continue-on-error 放行"。即当前实现为**阻断而非警告**，是有意的安全升级。旧待办条目作废；如需回退为警告须团队评审推翻 G1。

# P2 可暂缓（规模/多实例触发 或 远期新功能）

> 2026-08-07 全量代码核查结论：原 P2 共 29 项中，**13 项已因架构演进 / 已实现而过时**，已迁移至文件末尾「附录 A：已过时段归档」。下方仅保留**经代码核查仍真实存在**的 16 项，按"近期值得关注 → 已知接受/有意保留 → 远期新功能"重排序，优先级标签 P2 不变。

## 仍真实 · 近期值得关注（部分缓解但本质未解）

- ⚠️2026-07-31-EX-1 / R2：单实例部署、无灾备--**部分缓解**：已加每日 03:00 全量备份 sidecar（`docker-compose.yml` backup 服务，RPO 24h），但仍是单 PostgreSQL 主库、无 replica / 故障转移 / 多区域。`docker-compose.yml` 搜索无 replica/litestream/pgpool 痕迹。结论：风险接受（< 200 用户），多区域容灾见 P2/L3。
- ⚠️未标注日期-R17：审计日志丢失--**部分缓解**：审计已持久化 PostgreSQL `admin_actions`（AuditLog）；但 `audit_service.record` 默认 best-effort，失败仅 `logger.warning` 忽略，仅 `record_atomic(strict=True)` 才严格落库。默认路径下"可能丢失"仍成立。结论：风险接受（等级低），严格路径可用。
- ⚠️2026-08-01-P2/L4 + L5：外部监控接入 Sentry/GlitchTip + 性能埋点--**部分实现**：前端已可选接入 Sentry（`monitoring.ts` 动态 `ensureSentry()`，需 `SENTRY_DSN`）+ 错误率监控；后端**未接入 Sentry/GlitchTip**（无 `sentry_sdk` 依赖），但有 OpenTelemetry traces/metrics（OTEL 可选）作为替代。结论：后端 Sentry 接入仍真实待处理（支撑 P1 Ops-SLI/R18 外部探针）。
- ⚠️未标注日期-ErrorCode-演进：业务模块自治错误码--**仍真实待处理（已预留，未执行）**：错误码仍集中于 `app/core/exceptions/error_codes.py`，`app/services/*/errors.py` 搜索 0 文件；文件头与多处注释已预留"迁业务模块"路径。当前状态"暂不执行"仍成立。

## 仍真实 · 远期新功能（单实例 < 200 用户下不紧急）

- 2026-08-01-2.0 微服务：Python/Java 拆分（Part C）--**仍真实待处理（仅 MQ/arq 骨架，无拆分目录）**：全仓 0 个 `.java`；Auxilio 仍是后端单体内 service；无 Spring Boot 签到服务。阶段 0–4 均未启动。
  - Part C/阶段0：单体巩固--完成 Part A M10 + Part B（M10 前端 Repository 抽象层未做，见下 Devdocs-Arch-3.3 P1）
  - Part C/阶段1：Python 试点--Auxilio 迁 FastAPI，TS 调用
  - Part C/阶段2：Java 试点--签到服务迁 Spring Boot
  - Part C/阶段3：事件总线跨语言--appBus -> MQ
  - Part C/阶段4：全面拆分--按清单迁移
- 2026-08-01-P1/M1：关注/好友系统--**部分实现**：互相关注（`follow-button.tsx` + `/follows` 路由）+ 动态流（`/community/feed`）已实现；**私信（关注后才可私信）未实现**，全库无 dm/conversation 模块。结论：私信部分仍真实待处理。
- 2026-08-01-P1/M2：Wiki/知识库--规章制度/FAQ/新人指南，多人协同编辑。前端 src 搜 `wiki|知识库|协同` 0 命中，未实现，仍真实。
- 2026-08-01-P1/M4：活动评价与反馈--活动结束后评分 + 文字评价。事件模块无 rating/review 类型/API，未实现，仍真实。
- 2026-08-01-P1/M5：相册/活动回顾--活动照片上传、瀑布流展示。搜 `gallery|album|瀑布流` 0 命中，未实现，仍真实。
- 2026-08-01-P2/L1：内网聊天工具--站内实时消息。无 websocket/chat 会话模块（通知 ≠ 聊天），未实现，仍真实。
- 2026-08-01-P2/L2：RSS/邮件订阅--活动更新、文章发布 RSS + 邮件通知。搜 `rss` 0 命中（仅通用 feed/SWR），未实现，仍真实。
- 2026-08-01-P2/L3：多区域灾备--单节点 Docker，无区域容灾。deploy compose 搜 `multi-region|failover|replica` 0 命中，仍真实（与 EX-1/R2 同源）。
- 2026-08-01-P2/L7：数据归档冷存储--历史活动/日志归档。前端无归档代码；后端 `data_retention.py` 仅为清理非冷存储。未实现，仍真实。
- 2026-08-01-P2/L8：未读通知 SSE 推送--替代轮询。`notification-bell.tsx` 仍为 fetch 拉取未读数（轮询模式），搜 `EventSource|text/event-stream` 0 命中，未实现，仍真实。
- 2026-08-01-P2/L10：GraphQL 网关--聚合多模块查询。搜 `graphql` 0 命中，仍为 REST，未实现，仍真实。
- 2026-08-05-文档迁移-待办1：后端加 docs 只读托管/API + 前端 viewer 加外部 Tab（方案②）。后端 `main.py` 仅 FastAPI Swagger `/docs`（非业务文档托管）；前端 `dev-docs-viewer.tsx` 无外部 Tab。未做，仍真实。
- 2026-08-05-文档迁移-待办2：提炼跨仓库通用规范进前端 Devdocs-*（方案③，去重）。`tools/docs/` 下无 `Devdocs-*` 命名文件，未做，仍真实。
- 2026-08-05-文档迁移-可选：前端 DevCenter 加「后端文档源」Tab（可选增强）。`dev-center/page.tsx` 仅 `docs`/`registry` 两 Tab，未做，仍真实（可选）。

## 仍真实 · 架构优化（Devdocs 跟踪项）

- 2026-08-01-Devdocs-Arch-3.3：P1 待处理优化--M10 Repository 抽象层（前端无独立 Repository 抽象层，直接用 `backend-client.ts`）、M1 关注/好友（私信未做）、M2 Wiki、M4 活动评价、M5 相册（均见上）。
- 2026-08-01-Devdocs-Arch-3.3：P2 待处理优化--L6 PWA 离线（**已实现，见附录 A**）、L8 未读通知 SSE（见上）、L9 定时任务（后端已实现，前端无 UI 对应）。

---

# P3 观察 / 可选（非致命、有意保留或可选增强）

> 2026-08-07 核查结论：原 P3 共 9 项中，**1 项（i18n-环境2）已过时**迁移至附录 A；其余 8 项经代码核查仍真实（多为"有意保留"的设计决策记录）。

- 2026-08-05-前端设计-EmptyState：其余手写空状态保留，组件已就绪可渐进迁移。**已实现组件** `components/feedback/empty-state.tsx` 已导出并在少数页面使用；其余页面（dev-docs-viewer、forum 类等）仍手写，当前手写处已明显少于原 ~28 处。渐进迁移方向仍真实。
- 2026-08-05-前端设计-z-index：首页 z-10/z-50 已收编为 token（globals.css `--z-banner`/`--z-header`）；navbar `z-[45]` 有注释说明保留（高于 banner 低于 header）。**已收编完成，记录属实**。
- 2026-08-05-前端设计-内联样式：14 处内联 `style={{}}` 多为断点/动画动态值，搜 `style={{` 命中 14 处，与描述吻合。收敛收益低风险高，**有意保留仍真实**。
- 2026-08-05-前端评审-不做1：缺 ui/ 域的补层（join/user/notification）--`modules/notification/` 仅含 `types/`，`NotificationBell` 位于 `components/` 跨域复用，不补 ui/ 域。决策仍真实。
- 2026-08-05-前端评审-不做2：modules ui/ 补 index.ts 桶--全库直接导入（搜 `ui/index` 桶 0 命中），不补桶。决策仍真实。
- 2026-08-05-前端评审-不做3：重复状态徽章/提示条/筛选组/模态框壳收敛--无统一 `Badge` 组件，多套独立 badge（event-status-badge/roleBadgeClass/admin-join-panel 等）未收敛。决策仍真实。
- 2026-08-05-i18n-环境1：dev server 日志有 ReferenceError: preload is not defined（Next16+ 自定义 server + next-intl dev 期已知非致命错误）。`src/server.ts` 自定义 server + `dev-server.mjs` tsx watch；`preload` 实为 HSTS `Strict-Transport-Security ... preload` 字符串非函数调用。非致命判断仍真实。

---

> 本文件为项目未完成任务的唯一权威跟踪。
>
> 注意：各文档中的「提交前自查 Checklist」模板（如后端 BackDoc-Conv.md、前端 FrontDoc-Ops.md / FrontDoc-UID.md / RootDoc-FEArch.md、根级 docs/Onboarding.md 附录 A.5 中的未勾选 `[ ]` 清单）属于 PR 自检规范模板，非项目级待办任务，未纳入本文件。
>
> 已闭环任务的历史归档统一收归 `项目演变历史.md`；**P2/P3 过时段归档**见 `项目演变历史-0.9.8.md` 末尾「0.9.8 — P2/P3 待办核查 · 过时段归档（2026-08-07）」。本文件正文仅保留经核查仍真实存在的未完成任务。
