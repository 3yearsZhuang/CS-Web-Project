# ============ FztbuCS-Project 根级 .gitignore ============
# 通用排除规范（与子模块共享同一套公共段）。
# 子仓库（CS-Web-Backend / CS-Web-Frontend）各自 .gitignore 在公共段之外，
# 额外保留本语言的专属忽略项。

# ---- 公共段（根级 / 后端 / 前端 三者一致）----

# 环境变量（密钥不入库；模板 .env.example 需提交）
.env
.env.local
.env.*.local

# 日志
*.log

# 开发日志目录（含非 .log 文件时整体排除）
.devlogs/

# IDE / 编辑器 / 工具数据
.vscode/
.idea/
*.iml
*.swp
*.swo
*~
.codebuddy/
.trae-cn/
.workbuddy/

# OS
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
._*

# 运行时数据 / 备份（由 compose 卷挂载到根级 data/ backups/）
data/
backups/

# 交付概览（agent 工作产物，非正式文档；如需保留可在本地查看）
overview.md

# Redis 快照（运行时数据；可能含会话态，不入库）
dump.rdb
*.rdb
appendonlydir/

# 构建/CI 临时错误日志（散落根目录的非源码产物；本地排障用，不入库）
*_err.txt

# 注：pgdata 等为 Docker 命名卷，由 Docker 管理，
# 不落在工作区目录，无需在此忽略。

# ---- 根级无源码，公共段即全部；语言专属段见子模块 .gitignore ----
