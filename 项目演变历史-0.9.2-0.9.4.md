# 项目演变历史 · 0.9.2 – 0.9.4

> 版本范围：**0.9.1 发布后 → 0.9.5 版本号统一前的迭代期**（无独立 patch 版本号标注，归为 0.9.2–0.9.4 连续迭代）。
> 覆盖日期：2026-08-01（文档整合 + 前后端分离迁移历史）、08-02（Repository 收官）、08-03（前端重建）、08-04（前端架构准则）、08-05（数据迁移 / 方案B / 设计优化 / i18n / 文档合并 / 超长页面拆分）。
> 编号体系见 [`项目演变历史.md`](./项目演变历史.md)（索引）。
> 前后版本：← 0.9.1 ｜ → 0.9.5–0.9.7（见同名拆分文件）。

---

# 2026-08-01

## 文档体系整合

| 文档 | 变更内容 |
|---|---|
| FrontDoc-Arch.md | 合并原 Devdocs-architecture.md + Devdocs-api-reference.md（最后更新 2026-08-01） |
| FrontDoc-Ops.md | Part B SLO 与错误预算定义验证、Part C 运维 Runbook 合并完成（最后验证 2026-08-01） |
| FrontDoc-Sec.md | 安全文档定稿（最后更新 2026-08-01） |
| FrontDoc-Onboard.md | 原「入职指南 + 项目规则」，已于 2026-08-06 合并进根级 `docs/Onboarding.md`（附录 A 工程规则）并删除 |
| FrontDoc-UID.md | 同步组件目录重构 + 修复 community 路径重复（最后更新 2026-08-01） |

## 前后端分离迁移（历史归档）

> 目标：将 CS-Web-Frontend（Next.js 全栈单体）中的全部后端功能（server 层 + API 路由 + 数据库访问）分离到后端仓库（FastAPI WitchCat 脚手架），前端降级为纯 UI + BFF 薄转发。

### 已确认决策（ADR 摘要）

| # | 决策 | 说明 |
|---|---|---|
| D1 | 认证统一到后端 JWT | 采用脚手架原生 JWT 双 token；前端不再使用 session cookie；密码哈希由 scrypt 迁移到 bcrypt |
| D2 | 分阶段按模块交付 | 每模块独立验收（契约 + 数据 + API + 前端转发），禁止一次性全量搬迁 |
| D3 | 数据库：PG 唯一生产库 | Schema 唯一来源 = Alembic；SQLite 仅在迁移完成前作为前端开发 fallback |
| D4 | 前端降级为 BFF | `src/app/api/**/route.ts` 全部改为薄转发到本仓库；`src/modules/*/server` 迁移完成后删除 |

### 背景与目标

| 项 | 前端 CS-Web-Frontend（原为全栈单体） | 后端 CS-Web-Backend（FastAPI 脚手架） |
|---|---|---|
| 框架 | Next.js 16 App Router + React 19 | FastAPI 0.139 + SQLAlchemy 2.0 async |
| 数据库 | SQLite（better-sqlite3，生产在用）；Drizzle PG 双引擎 schema 已到 Phase 1 | PostgreSQL（asyncpg，专属库 `domefff`）+ Alembic |
| 认证 | session cookie + 自实现 scrypt/HKDF/AES-256-GCM + TOTP + GitHub OAuth | JWT 双 token（access 15min / refresh 7day）+ bcrypt + Token 黑名单 |
| 业务规模 | 9 模块、66 个 server 文件、~140 个 API 路由、36 张业务表 | 现有 auth / users / rbac / audit 4 个模块、6+ 张表 |
| 横切面 | 内存限流、进程内事件总线、pino 日志 | loguru、可降级 Redis 限流/缓存、arq 队列、OTel、健康检查 |

### 目标架构（演进痕迹）

```
浏览器
  │ HTTPS
  ▼
Next.js 16（UI 层）── 保留全部 page/SSR/RSC/组件；route handler 变薄转发
  │ REST/HTTP + Authorization: Bearer <JWT>（X-Request-Id 透传）
  ▼
后端 FastAPI /api/v1（统一前缀挂载）
  ├── middleware 链：CORS -> 异常 -> 安全头 -> 日志 -> 指标 -> 限流 -> 认证限流
  ├── api -> service -> repository -> model（单向分层）
  ├── RBAC：require_permission("res","act") + PermissionChecker 旁路
  └── 能力：JWT 双 token / 黑名单 / TOTP / OAuth / 限流 / 缓存 / arq 队列 / OTel / 健康检查
  ▼
PostgreSQL（domefff）← Alembic 管理全部 42+ 张表（含前端 36 张业务表）
Redis（可选增强）── 限流 / 缓存 / 2FA 防重放 / 跨实例黑名单
```

### 表清单映射（SQLite/Drizzle 36 张 -> SQLAlchemy 模型）

| 模块 | 表 | 建议模型文件 `app/models/` | 状态 |
|---|---|---|---|
| 认证/用户 | users（含业务字段 display_name/bio/avatar/points/…） | `user.py` | **合并**：以现有 user.py 为基，补齐业务字段 |
| | sessions | `user_session.py`（若保留会话记录能力）或废弃 | 待定 |
| | login_history | `login_history.py` | 新建 |
| | password_history | `password_history.py` | 新建 |
| | verification_codes | `verification_code.py` | 新建 |
| | password_reset_requests | `password_reset_request.py` | 新建 |
| 框架已有 | roles / permissions / user_roles / role_permissions | 已有 `role.py` `permission.py` | 已有 |
| | refresh_tokens | 已有 `refresh_token.py` | 已有 |
| | exception_logs | 已有 `exception_log.py` | 已有 |
| | audit_logs | 已有 `audit_log.py` | 已有（与前端 admin_actions 合并） |
| 系统 | settings | `setting.py` | 新建 |
| | component_registry_items / _variants / _guides | `component_registry.py` | 新建 |
| | resources | `resource.py` | 新建 |
| 入社 | join_applications | `join_application.py` | 新建 |
| 活动 | events / event_registrations / event_checkins / activity_participations | `event.py` | 新建 |
| 论坛 | forum_categories / forum_topics / forum_replies / forum_likes / forum_favorites / forum_topic_views / forum_mentions | `forum.py` | 新建 |
| | forum_topics_fts（FTS5 虚拟表） | 不建 ORM；PG 用 GIN + tsvector 迁移 SQL | 新建 |
| 博客 | blog_posts / blog_series / blog_likes | `blog.py` | 新建 |
| 通知 | notifications / announcements | `notification.py` `announcement.py` | 新建 |
| 考试 | exams / exam_questions / exam_question_options / exam_attempts | `exam.py` | 新建 |
| 任务/积分 | tasks / task_claims / points_transactions | `task.py` `points.py` | 新建 |

> **迁移方式**：一次 Alembic baseline，把所有新模型一次性纳入首个大迁移 `add_cs_business_tables`，后续模块迭代增量迁移。禁止 `create_all`。
> **类型映射注意**：`integer` 布尔 0/1 -> SQLAlchemy `Boolean`；ISO 字符串日期 -> `DateTime(timezone=True)`；partial unique index -> 迁移中手工 SQL；JSON 列 -> `JSONB`。

### 横切面能力迁移清单

| 能力 | 前端现状 | 本仓库能力 | 动作 |
|---|---|---|---|
| 认证 | session cookie + scrypt | JWT 双 token + bcrypt + 黑名单 | 已就绪 |
| RBAC | 6 角色 × 20+ 权限点 | RBAC 完整 CRUD + require_permission | 权限点 seed 对齐 |
| 速率限制 | 内存 Map | 可降级 Redis/内存限流 | 阈值对齐 |
| 事件总线 | 进程内 EventEmitter | arq 队列（可选）+ eager 兜底 | 事件定义移植 |
| 2FA 防重放 | 内存 consumed-jti Set | Redis SET + TTL | 已具备 |
| 审计 | `admin_actions` 手动埋点 | `audit_logs` + exception 落库 | 表合并 |
| 日志/链路 | pino + X-Request-Id | loguru + 请求日志 | 透传 X-Request-Id |
| 上传/静态文件 | 磁盘直存 | multipart + 魔数校验 + 静态托管 | 已补齐 |
| 邮件 | nodemailer（SMTP） | aiosmtplib + arq 异步 | 已补齐 |
| 健康检查 | `/api/health` | `/health` `/readyz` `/status` | 已就绪 |

### 风险与缓解

| 风险 | 影响 | 缓解 |
|---|---|---|
| JWT 切换导致全站登出 | P0 | Phase 1 先行独立验收；迁移窗口双登录态并存 |
| scrypt->bcrypt 迁移失败 | P0 | 懒升级 + 迁移脚本双保险 |
| FTS5->tsvector 搜索差异 | P1 | Phase 4 单独验收搜索排序/中文分词 |
| 双库写入期数据不一致 | P1 | 每模块完成才切流量；单写后端 |
| 契约漂移 | P1 | 契约比对脚本；错误码走 `ErrorCode` 注册表 |
| 上传/邮件能力缺失 | P1 | Phase 2 前补齐基础能力 |
| 单进程->多 worker 语义变化 | P1 | Redis 化，生产启用 `REQUIRE_REDIS_FOR_SECURITY` |

### 迁移阶段执行记录

| 阶段 | 变更内容 |
|---|---|
| Phase 0 | 全量 36 表 SQLAlchemy 模型 + 首个 Alembic 基线迁移 d1e2f3a4b5c6（33 张新表 + users 扩展 8 业务字段） |
| Phase 0 | 决策闭环--OQ-1 统一 audit_logs / OQ-2 扩展后端 users / 主键全部 Integer |
| Phase 1 | 邮箱登录 + 公开注册（验证码）+ 忘记密码申请流 |
| Phase 1 | TOTP 2FA 全流程（RFC 6238 + Node 交叉验证）+ GitHub OAuth + 邮件发送 |
| Phase 1 | scrypt->bcrypt 懒升级 + 2FA secret 同算法加密迁移 |
| Phase 1 | profile CRUD + 头像 + 公开主页 + 改密（历史复用检测） |
| Phase 1 | 登录历史 + 设备列表/远程登出 + 密码重置审批 |
| Phase 1 | 前端 BFF 切换（19 个路由薄转发） |
| Phase 1 | OQ-3 BFF 托管 httpOnly cookie / OQ-4 TOTP secret 加密迁移 / OQ-5 密码哈希懒升级 / OQ-7 refresh 静默刷新 |
| Phase 2 | announcement / notification / join + admin users 管理（保护规则全量移植） |
| Phase 2 | 事件总线 app/core/events.py + 通知订阅者 + 角色 seed |
| Phase 2 | 前端 BFF 16 个路由薄转发 |
| Phase 2.5 | roles 表扩展 + /admin/roles CRUD + 权限全量替换 + 审计删除 |
| Phase 3 | 活动 CRUD + 报名（限额/去重）+ 签到核销 + 自动归档 + 批量/统计 + 设置 |
| Phase 3 | 前端 BFF 15 个路由薄转发 |
| Phase 4 | 论坛（版块/主题/回复/点赞收藏/浏览去重/@提及）+ 审核 + 博客 + 成员名录 + Feed 聚合 |
| Phase 4 | 图片上传 + 搜索降级（FTS5 -> ILIKE） |
| Phase 4 | 前端 BFF 37 个路由薄转发 |
| Phase 4 | OQ-6 上传文件存储--初期本地磁盘 |
| Phase 5 | 考试（组卷/判分/排名）+ 资源（提交/审核）+ 任务（认领/审核->积分）+ 积分（流水/排行榜/等级） |
| Phase 5 | Auxilio 学习助手 + 组件注册表 |
| Phase 5 | 前端 BFF 20 个路由薄转发 |
| Phase 5 | 数据迁移脚本（SQLite -> PG 全量导出导入）已完成 |
| Phase 6 | 论坛 + 成员全文检索 GIN + tsvector 优化（替代 Phase 4 的 ILIKE 降级）：`community_posts`/`users` 加 `search_vector` 列 + GIN 索引 + 触发器自动维护，搜索改 `websearch_to_tsquery` |
| Phase 6 | 事件总线跨实例（ADR-014 落地）：`MULTI_INSTANCE=true` 时经 arq/Redis 广播事件，worker 实例本地订阅者落地执行；默认 `false` 单实例零开销。`docker-compose.yml` 新增 `redis`+`worker` 服务 |

## 后端设计稿落地（历史归档）

> 仅作演进痕迹保留。现行实现细节见 `docs/BackDoc-Sec.md`（异常）、`docs/BackDoc-Infra.md`（日志）。

| 系统 | 变更内容 |
|---|---|
| 异常处理系统 | app/core/exceptions/ + BaseAppException 子类体系 + setup_exception_handlers + ExceptionHandlerMiddleware + exception_log 表 + ErrorCode 注册表（已完成，设计方向与实现一致） |
| 日志系统 | app/core/loguru_logger/ + LoguruAdapter + set_logging_context / ContextVar + 日志轮转/异步写入/敏感信息脱敏（已完成，但技术选型从 structlog+orjson+PostgreSQL 改为 loguru 无 DB 落库） |

---

# 2026-08-02

| 编号 | 变更内容 |
|---|---|
| ADR-009 | Repository 抽象层全量迁移收官--服务层彻底去除 getDb() 直接调用，统一经 Repository 访问数据；覆盖 auth/user/community/events/tools/notification/admin/announcement 全部模块及 16 个 API 路由 |

---

# 2026-08-03

| 编号 | 变更内容 |
|---|---|
| 历史归档 | 前端重建--适配上游 community v2 重构（统一 topic/post 一表，论坛+博客合并 + 关注/举报） |

---

# 2026-08-04

| 编号 | 变更内容 |
|---|---|
| RootDoc-FEArch.md | 根级前端通用目录/架构准则定稿（由前端 FrontDoc-General.md 迁移为唯一权威） |

---

# 2026-08-05

## 数据迁移（SQLite -> PostgreSQL）

| 编号 | 变更内容 |
|---|---|
| 数据迁移 | 迁移成功完成，业务数据全部入库，外键完整、无孤儿记录、类型转换正确 |
| 数据迁移 | 逐表行数对比 SQLite 全部一致（19 张表，含 users PG=16/SQLite=15） |
| 数据迁移 | 外键完整性 9 组检查 0 孤儿，主键重映射完整 |
| 数据迁移 | 全部 UUID/TEXT 主键 -> PG Integer 自增序列完成映射，自增序列已 setval 同步 |
| 数据迁移 | 认证字段迁移完成--角色单列->user_roles 多对多；username 派生；scrypt 哈希直搬 + 懒升级 bcrypt；2FA 迁移成功 |
| 数据迁移 | 类型转换完成--布尔/日期/JSON/组件 item_id 回填 |
| 数据迁移 | API 序列化对齐--events 列表字段 null->[] 修复 |
| 数据迁移 | 静态资源迁移--avatars（5 个）+ forum-images（1 个）已复制到后端；论坛图片路径已修正 |
| 数据迁移 | 验证记录全部通过（7 项--行数/FK/角色/关联/联查/唯一约束/健康检查） |
| 数据迁移 | 取舍清单执行--舍弃 sessions/login_history/admin_actions/FTS 虚拟表/community_*views；保留 notifications/join/announcements/tasks/points/2FA |
| 迁移脚本 | tools/scripts/migrate-sqlite-to-pg.mjs 已实现--UUID->Integer 主键重映射 + 依赖序导入 + 类型转换 + 序列 setval + 幂等/RESET + DRY RUN |
| 迁移文档 | tools/docs/FrontDoc-PGMig.md 迁移脚本唯一权威使用说明 |

## 方案B（统一 camelCase 传输契约）

| 编号 | 变更内容 |
|---|---|
| 方案B-决策 | 用户选定方案B（后端统一 camelCase HTTP 传输契约），一并移除前端 SQLite、测试同步改 camelCase、手写 dict 收敛 response_model、按模块分波 |
| 方案B-波次0 | app/schemas/base.py 新增 camel_config()，TZModel 改用之--所有继承 TZModel 出参自动 camelCase |
| 方案B-波次1-后端 | auth/profile/user schema 全部加 camel_config()；新增 SessionOut/SessionListResponse，/auth/sessions 加 response_model |
| 方案B-波次1-测试 | test_auth.py 5 处 HTTP 响应断言改 camelCase；test_http_postgres_e2e.py 同步 |
| 方案B-波次1-前端 | backend-client.ts 接口改 camelCase；BFF 路由 auth/* + profile/* + admin/users/[id] 字段改 camelCase；前端测试 backend-client.test.ts 修复 |
| 方案B-验证 | 后端 GET /auth/me 返回 200 user 字段全 camelCase；前端 tsc --noEmit 通过、backend-client 单测 10/10 |
| 方案B-遗留1已解决 | ErrorResponse 统一 camelCase--safe_json_response 改 model_dump(by_alias=True)；后端测试 3 文件 HTTP 错误体断言改 camelCase；前端 BackendErrorBody + normalizeError + oauth/github/callback 改 errorCode |
| 方案B-遗留2已解决 | 分页查询参数 pageSize--admin_users.list_users 和 community.list_follows 两端点 page_size 加 Query(alias="pageSize")；前端 admin/users/route.ts 和 community/forum/follows/route.ts 发 pageSize |
| 方案B-文档 | 改造计划文档更新--新增「0.5 实施进度总览」表 + 各波次状态标记 + 「8. 已完成改动清单」+ 更新「7. 待确认」 |
| 方案B-修复 | app/models/blog.py 缺 from typing import Optional 已补 import（预存 bug） |
| 方案B-camelCase 联调缺陷（9 项，实测发现并修复） | 1) 2FA setup 500：service 返回改 snake_case `otpauth_uri`/`backup_codes`；2) 2FA 登录分支失效：BFF 改读 `requires2Fa`；3) 2FA 登录第二步必 401：新增 `get_optional_current_user` 依赖替换 4 处；4) 2FA verify 缺 response_model：补 `response_model=LoginResponse`；5) sessions 字段错位：BFF 改读 camelCase；6) 预设头像契约错位：重写 `preset/route.ts`；7) 密码重置列表空：重写 list 映射 + approve 补 `jsonBody:{}`；8) 改密 422：BFF 改发 `{oldPassword,newPassword}`；9) 错误响应体命名不一致：`exception_middleware.py` 改 `by_alias=True`，`rate_limit.py`/`body_limit.py` 错误体改 camelCase |

## 前端构建修复

| 编号 | 变更内容 |
|---|---|
| 前端构建修复 | 从 src/shared/utils/index.ts 移除 image-utils（server-only）与 mail（nodemailer）两条导出，修复客户端构建报错 |
| 前端构建修复 | 约定写入文件头注释--server-only 模块禁止经 barrel 导出 |

## 全栈精简/清理

| 编号 | 变更内容 |
|---|---|
| 清理-低风险 | 删除死依赖 qrcode+@types/qrcode、drizzle-orm+drizzle-kit；删除 drizzle.config.ts + src/shared/db/drizzle/ 整目录；删除 codemod 脚本 10 个、Litestream 脚本 2 个（+package.json 2 条 db:* script）；mask.ts 三函数去导出（改私有）、formatDate 统一为 utils.formatDateTime |
| 清理-锁文件 | pnpm install --lockfile-only 同步，4 个死依赖从 pnpm-lock.yaml 完全移除 |
| 清理-Litestream | 前端 tools/deploy/ 的 Dockerfile 删 Litestream 安装段 + litestream.yml COPY、docker-entrypoint.sh 删 Litestream 段、litestream.yml 整删 |
| 后端-小重构 | _post_out 合并为 schemas/community.py 的 post_to_dict（community.py + admin_community.py 复用）；alembic upgrade 逻辑合并（database.run_alembic_upgrade 公共化，db_initializer.apply_migrations 复用）；requirements.txt 补 greenlet>=3.0,<4.0 与 pyproject.toml 对齐，lint 0 错误 |
| 后端-旧模型删除 | 删 forum.py/forum_interaction.py 整文件，blog.py 仅留 BlogSeries（删 BlogPost/BlogLike），__init__.py 与 schemas/community.py 同步清理 |
| 后端-附带修复 | points.py 的 Mapped[int | None]（Python 3.9 不兼容）改为 Optional[int] + 补 typing.Optional 导入 |
| 文档同步 | 根级 README.md 与 docs/RootDoc-Deploy.md 已更新架构事实（前端不持有业务数据、Litestream 已移除、SQLite 仅历史迁移源） |

## 前端设计优化（P0-P3 全部落地）

| 编号 | 变更内容 |
|---|---|
| 前端设计-无障碍 | globals.css 新增全局 reduced-motion 降级块；新增 :focus-visible 全局焦点环兜底；彩蛋改为 triggerEggAt + sr-only 键盘触发按钮 |
| 前端设计-token 纠错 | 新增 --destructive-foreground；.btn-danger 文字色修正；Button 移除冗余 disabled:opacity-50；删除重复类 card-hover、死类 gradient-text/glow-blob |
| 前端设计-性能 | 字体 preconnect 预连接；.ark-scanline 移除 mix-blend-mode + opacity 调整 + reduced-motion 下隐藏；深色首帧闪烁根治 |
| 前端设计-可读性 | .tag-badge 10px->11px、.btn-outline-sm 10px->11px |
| 前端设计-设计系统 | 新增统一 EmptyState 组件，迁移 notifications/events/community-post-list 三处空状态 |
| 前端设计-校验 | tsc --noEmit 通过（仅 i18n en.ts 预存错误）；eslint 改动文件 0 error |
| 前端设计评审 | 13 项修复全部落地（P0 reduced-motion/彩蛋键盘/focus-visible；P1 token/disabled/字体；P2 scanline/字号/z-index；P3 lang/闪烁/EmptyState/彩蛋溢出） |

## i18n 国际化迁移

| 编号 | 变更内容 |
|---|---|
| i18n-基础设施 | src/i18n/request.ts、types.ts、zh-CN.ts、en.ts、layout.tsx、next.config.ts、language-switcher.tsx 已完成 |
| i18n-第六轮 | 修复 mask.test.ts 预存类型错误；管理后台 /admin 容器翻译（admin namespace） |
| i18n-第七轮 | 新增 adminUsers namespace（约 60 key），迁移 user-modals.tsx 全部 useTranslations |
| i18n-第八轮 | user-list-view.tsx（约 56 处）翻译；app/community/[id]/page.tsx 社区帖子详情主页面翻译 |
| i18n-文档 | 生成 FrontDoc-i18n.md（128 行），记录架构决策、基础设施、已完成迁移、标准迁移流程、剩余清单、已知问题；已登记到 README.md 文档索引 |
| i18n-累计 | 11 个主页面 + LanguageSwitcher + admin 用户管理（user-modals + user-list-view）+ 社区帖子详情主页面 |

## 前端代码评审修复（FrontDoc-Review.md）

| 编号 | 变更内容 |
|---|---|
| 修复项1 | 公告面板收敛到 announcement 域唯一实现；删除冗余 admin/ui/announcements-panel.tsx |
| 修复项2 | FilterBar / InlineTabs / SectionNav 及其类型补入 components/index.ts primitives 桶导出 |
| 修复项3 | use-admin-users.ts（538 行）按关注点拆为三 Hook（use-user-list / use-user-resets / use-admin-users 组合层） |
| 修复项4 | FilterBar 解耦 events 业务域（dotClassName?: string，删除反向 import） |
| 修复项5 | shared/config 桶接入业务--补全 auth-constants 导出，10 处直接子文件导入改为 @/shared/config |

## 文档合并与根级 docs

| 编号 | 变更内容 |
|---|---|
| 文档合并-P0 | FrontDoc-General.md 提炼为根级 RootDoc-FEArch.md（唯一权威） |
| 文档合并-P1 | 根级 docs/RootDoc-Deploy.md + 索引建立 |
| 文档合并-P1 | 迁移记录收进 docs/data-migration/（注：该子目录后续已合并提升至 `docs/RootDoc-MigEval.md`，`docs/data-migration/` 不再独立存在） |
| 文档合并-P3 | 根 README.md 文档地图建立 |
| 文档迁移-决策 | 后端 docs 留在后端仓库（方案①）；根目录建 docs/ 只放跨项目文档 + 索引/链接（方案③ 推荐） |
| 部署拆分 | 根级全栈 docker-compose.yml + Makefile + deploy/caddy/ + 两个 submodule 已落地 |
| 数据迁移评估 | 迁移可行性评估完成--数据可完整迁移但非直迁，需专用映射脚本 |
| 多库支持评估 | 前端已原生支持 SQLite/PG 双引擎；后端仅 PG，不引入多库 |

## 根级文档事实更新

| 文档 | 变更内容 |
|---|---|
| 根README | 前端已移除本地 SQLite 主存储与 drizzle 配置，全部业务数据由后端 PostgreSQL 承载 |
| docs/RootDoc-Deploy.md | 根级部署文档建立，覆盖本地并行启动、容器化全栈部署、健康检查、回滚、数据卷与备份；前端 Litestream/SQLite 备份体系已移除 |

## 文档合并清理（RootDoc-MergeGuide 执行，2026-08-05）

| 文档 | 原因 | 结论摘要 |
|---|---|---|
| FrontDoc-General.md（已删除） | 纯指针（空桩） | 前端通用目录/架构准则已迁移至根级 `docs/RootDoc-FEArch.md` 作为唯一权威 |
| RootDoc-MigLog.md（已删除） | 空桩 | SQLite → PostgreSQL 迁移执行记录已收归本文件，无需独立桩文档 |
| FrontDoc-Review.md（已删除） | 一次性结构审查报告 | 前端结构审查发现 3 个高危问题（公告面板重复实现、primitives 桶导出缺口、use-admin-users 超限），均已修复并落地至上方"前端代码评审修复"条目 |
| RootDoc-前端设计评审报告.md（已删除） | 一次性设计评审报告 | 设计评审通过 AI Slop Test；Token 纪律/字体体系/状态基础设施三方面表现出色，主要收尾项为执行细节；评分见设计评审报告 |

## 前端内容设计规范合规审计与整改

| 项 | 结论 |
|---|---|
| 审计结论 | 对照 `docs/RootDoc-FEArch.md`「二、内容设计规范」，**未完全做到**。2.1（原子分层）/2.3（配置即数据）/2.5（样式红线 `!important` 仅 reduced-motion）/2.6（接入仪式）合规；2.2（展示/容器分离）、2.4（复用阈值：文件 >500 行必须拆、useState >10 / 逻辑 >150 行提 Hook）**明确违规** |
| 关键证据 | 实测 `app/*/page.tsx` 至少 8 个文件 >500 行且 UI+业务逻辑耦合（未拆 Hook/store），违反 2.2/2.4 |

### 超长页面拆分整改（落实 2.4 / 2.2，全部已验证）

> 统一范式：抽 `useXxx` Hook（状态+逻辑，落 `modules/<域>/ui/hooks/` 或页面就近）+ 拆展示子组件（tab/区块）+ 父页面仅装配、以 `{...hook}` 注入。整改后全部 <500 行。

| 页面 | 原行数 | 整改后 | 新增 Hook | 新增子组件 | 单测 |
|------|------|------|-----------|-----------|------|
| `profile` | 1303 | 290 | `useProfile` / `usePassword` | `profile/security/activity/join-tab` + `session-manager` | 9 passed |
| `tools/task` | 798 | 101 | `useTasks` | `board/my-claims/points-tab` + `task-shared` | 5 passed |
| `login` | 744 | 76 | `modules/auth/ui/hooks/use-auth-form` | `auth-form` / `two-factor-form` / `forgot-password-form` | 5 passed |
| `tools/resource` | 704 | 249 | `use-resources` | `resource-card` / `submit-resource-modal` | 4 passed |
| `community` | 653 | 381 | `use-community-feed` | 复用既有 sidebar / card 子组件 | 3 passed |
| `community/new` | 526 | 169 | `use-compose` | `compose-form` | 4 passed |
| `tools/exam/[id]` | 513 | 121 | `use-exam` | `question-list` / `question-panel` | 4 passed |
| `notifications` | 501 | 125 | `use-notifications` | `notification-center` | 4 passed |

| 项 | 结论 |
|---|---|
| 验证 | `tsc --noEmit` exit 0；`eslint --quiet` 全部改动目录无 error；`vitest` 本次新增 7 文件 **29 passed** + 既有 profile/password **9 passed**，无回归 |
| 测试模式 | `mock next-intl(useTranslations)` + `next/navigation`（稳定 router 对象避免更新风暴）+ `fetch` 按 URL 返回；community 测试额外 mock `use-collapsing-hero` 隔离 jsdom 滚动/RAF 风暴 |
| 既有纪律 | 既有的 `components/`（primitives/layout/effects/feedback）分层与 `modules/` 桶导出纪律未改动，仅对 `app/` 路由页拆分 |
