# 项目演变历史 · 0.9.1

> 版本范围：**0.9.1 发布**（含发布前基线）。
> 覆盖日期：2026-07-29（架构决策基线）、2026-07-31（0.9.1 版本发布）。
> 编号体系见 [`项目演变历史.md`](./项目演变历史.md)（索引）。
> 后续版本：0.9.2–0.9.4 → 0.9.5–0.9.7 → 0.9.8（见同名拆分文件）。

---

# 2026-07-29

| 编号 | 变更内容 |
|---|---|
| ADR-001 | 采用 Next.js 全栈框架（已实施） |
| ADR-002 | better-sqlite3 作为主数据库（已实施） |
| ADR-003 | 模块化分层架构，9 个业务模块（已实施） |
| ADR-004 | 权限模型采用 RBAC（已实施） |
| ADR-005 | SQLite 写瓶颈应对策略（已实施） |
| ADR-006 | 事件驱动通知系统，事件总线跨模块通信 appBus（已实施） |
| ADR-007 | Markdown 渲染安全策略，rehype-sanitize 白名单净化（已实施） |
| ADR-008 | API 无版本前缀 + 向后兼容（已实施） |
| ADR-011 | 模块化权限检查，细粒度 RBAC（content_moderator / exam_admin / task_publisher）（已实施） |
| ADR-012 | 单节点部署架构（已实施） |
| UI-§3.4 | 新增子页面返回按键规范；为 /tools/exam、/tools/resource 添加 ← 返回 按键 |
| 安全审计 | Part A OWASP Top 10 安全审计完成（审计日期） |
| ADR-013 | 事件监听器显式初始化，通知监听器迁至 src/instrumentation.ts（已实施） |
| Devdocs-Ops | Part A 部署指南合并定稿 |
| Devdocs-Arch | 17.1 健康检查端点 /api/health（对应 Q5） |

---

# 2026-07-31

## 0.9.1 版本发布

### 功能新增（Added - 功能）

| 编号 | 变更内容 |
|---|---|
| 0.9.1 | 论坛系统--版块 -> 主题 -> 回复 -> 楼中楼，Markdown 编辑，点赞/收藏，@提及，搜索 |
| 0.9.1 | 博客/技术文章系统--Markdown 发布，系列管理，目录导航 |
| 0.9.1 | 内网考试系统--选择题 + 自动判分 + 排名，管理员组卷 |
| 0.9.1 | 学习资源站--分类浏览，提交审核，文件上传 |
| 0.9.1 | 协会任务发布页--任务领取，审核，积分联动 |
| 0.9.1 | Auxilio 学习成长 Agent--规则引擎，考试数据 -> 薄弱标签 -> 资源推荐 |
| ADR-016 | 活动系统--CRUD，报名表单定制，签到码核销，自动归档，月历视图 |
| 0.9.1 | 用户公开主页 + 技术档案 |
| 0.9.1 | 成员名录/技术墙 |
| 0.9.1 | 入社申请线上化 |
| 0.9.1 | 全站公告/置顶 |
| 0.9.1 | 站内通知系统 |

### 安全加固（Added - 安全）

| 编号 | 变更内容 |
|---|---|
| ADR-015 | TOTP 双因素认证（管理员强制）；GitHub OAuth 登录（已启用 2FA 者强制二次验证）；2FA 预认证 token 防重放（jti 消费 + 5min 清理）；TOTP 密钥 HKDF-SHA256 派生 |
| M6 | 密码策略升级--最小 8 位 + 复杂度校验 + 弱密码黑名单 + 历史密码复用检测 |
| 0.9.1 | 细粒度 RBAC--content_moderator / exam_admin / task_publisher |
| F2 | 全站安全响应头--CSP nonce 化 / HSTS / X-Frame-Options / Referrer-Policy / Permissions-Policy |
| 0.9.1 | 速率限制精细化 |
| M7 | 敏感数据脱敏--email/phone/name/studentId |
| 0.9.1 | SQL 注入全面审计（100% prepared statement） |
| 0.9.1 | 统一输入验证框架（zod） |
| 0.9.1 | Markdown 渲染白名单净化（rehype-sanitize） |
| 0.9.1 | 对象级权限（IDOR 防护） |
| 0.9.1 | 登录历史与异常告警（login_history 表） |
| 0.9.1 | 会话管理增强（设备列表、远程登出） |
| 0.9.1 | 高危操作二次确认（密码二次确认守卫） |
| Q4 | 依赖漏洞扫描命令可用（pnpm audit 可执行） |
| 0.9.1 | Cookie __Host- 前缀（生产强制 Secure + Path=/） |
| 0.9.1 | 论坛图片读取端点 session 访问控制 |
| 0.9.1 | 安全审计日志增强 |

### 架构与工程质量（Added - 架构）

| 编号 | 变更内容 |
|---|---|
| 0.9.1 | 模块化架构--9 个模块；事件总线跨模块通信（appBus） |
| 0.9.1 | Litestream 流式备份 |
| Q4 | pino 结构化日志（NDJSON） |
| Q5 | 健康检查端点（/api/health） |
| Q6 | 请求 ID 注入（X-Request-Id） |
| 0.9.1 | 错误率监控告警；可选 Sentry 接入（SENTRY_DSN 动态）；SLO 定义与 error budget 管理；运维 runbook；Litestream restore drill 脚本；k6 负载测试脚本 |
| 0.9.1 | CI 集成 build 验证 + 依赖审计 |
| 0.9.1 | 业务模块单元测试 308 条；E2E 业务流程断言 25 条（Playwright）；API 集成测试；数据库迁移工具 |
| ADR-009 | Repository 抽象层 |
| 0.9.1 | 用户等级/积分系统；共享审核工作流提取；组件扁平化与子目录拆分 |
| 0.9.1 | Git hooks + CI 流水线；441+ 单元测试全绿 |

### 变更（Changed）

| 编号 | 变更内容 |
|---|---|
| Q2 | 统一错误处理模式（AppError + ERROR_STATUS_MAP） |
| Q3 | 提取 EASE 动画常量（--ease-ark CSS 变量） |
| F1 | 19 个页面统一使用 CollapsingHero |
| F3 | 安全头迁移至 proxy.ts 统一入口 |
| ADR-010 | server-only 边界澄清 |
| 0.9.1 | AuditContext 类型下沉至 shared/types |
| 0.9.1 | 论坛/博客/成员模块合并为 community |
| 0.9.1 | App 路由重组；API 路由重组；类型统一定义；文件结构精简 |
| 0.9.1 | Sentry 依赖移除；template.tsx 删除；security.test.ts 迁移；未使用 import 清理 |
| Q1 | TopicDetail 拆分（主组件 191 行，拆 3 子组件 + 2 hook） |
| 0.9.1 | 生产启动强制校验（AUTH_SESSION_SECRET / ALLOWED_ORIGINS） |
| 0.9.1 | 模块级权限守卫 requireModuleAdmin 落地，19 路由迁移 |
| M3 | 活动日历视图（月历 + 年度时间轴双视图） |
| M11 | 客户端/服务端边界澄清 + AuditContext 下沉 |

### 缺陷修复（Fixed）

| 编号 | 变更内容 |
|---|---|
| ADR-016 | events.date 自由格式与 ISO 时间戳字典序比较缺陷 |
| ADR-017 | 跨模块日期格式比较缺陷 |
| ADR-015 | 2FA 端点缺少限流 + Origin 校验；GitHub OAuth 绕过 2FA；密码重置多步操作未包事务致旧 session 残留；默认重置密码硬编码弱口令；events 表迁移未包事务；启动失败与未处理 rejection 无结构化日志 |
| ADR-013 | 事件监听器隐式初始化致通知静默失效 |

### 安全审计修复（Devdocs-Sec Part A + D）

| 轮次 | 变更内容 |
|---|---|
| 汇总 | 全部严重/高危/中危/低危漏洞修复（🔴严重 0 / 🟠高 4 ✅ / 🟡中 7 ✅ / 🟢低 5 ✅） |
| 第一轮（ADR-015） | 2FA 四路由补齐 assertAllowedOrigin；twoFactorSetupLimiter（3/分）；生产缺 AUTH_SESSION_SECRET 即 FATAL 退出；生产缺 ALLOWED_ORIGINS 即退出；预认证 token（jti 防重放）替代密码二次传输；twoFactorLimiter（5/分）覆盖 verify；GitHub OAuth 强制 2FA；OAuth token 改 __Host-oauth_2fa HttpOnly cookie；backup-codes 补齐 Origin 校验 + 限流 |
| 第二轮 | ROLE_MODULE_MAP + requireModuleAdmin；HKDF-SHA256 派生 TOTP 密钥 + 强制 TOTP_ENCRYPTION_KEY；架构文档标注单进程假设与多实例迁移清单；audit.yml（high+ 阻断）；论坛图片 API session 校验 + private 缓存；buildCsp(nonce) 生产移除 unsafe-eval/inline；AUTH_COOKIE_NAME 生产 __Host-auth_session；recordLoginHistory 扩展 + 迁移 v6；集成 pino + createRequestLogger，33 模块替换 console.error；rehype-sanitize 默认白名单审查 |

### 基础设施（Infrastructure）

| 编号 | 变更内容 |
|---|---|
| 0.9.1 | Docker + Caddy 部署方案；Litestream 本地/S3 备份；Cloudflare Tunnel 内网穿透 |

### 文档（Documentation）

| 编号 | 变更内容 |
|---|---|
| 0.9.1 | FrontDoc-Ops.md（合并文档）；根级 docs/Onboarding.md（合并原 FrontDoc-Onboard.md）；FrontDoc-Arch.md |
| ADR-001~019 | ADR 记录 19 条，其中 ADR-001~013、ADR-015 等均已实施；**ADR-014（事件总线跨实例异步化）已于 2026-08-06 落地**：后端 `app/core/events.py` 在 `MULTI_INSTANCE=true` 时经 arq/Redis 广播事件，每个 worker 实例在 `on_startup` 注册本地订阅者并落地执行；单实例默认 `false`（零开销，行为不变）。 |
| R1-R20 | 风险登记表 |

### 测试验证

| 项 | 结果 |
|---|---|
| 安全审计 | pnpm test 441/441 passed（13 files，2.72s） |
