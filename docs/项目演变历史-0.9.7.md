# 项目演变历史 · 0.9.7

> 版本范围：**0.9.7 版本号演进 + 待办闭环汇总（P0/P1/P2）+ 真伪核查归档**。
> 覆盖日期：2026-08-06。
> 编号体系见 [`项目演变历史.md`](./项目演变历史.md)（索引）。
> 前后版本：← 0.9.6（见同名拆分文件）｜ → 0.9.8（见同名拆分文件）。

---

# 2026-08-06

## 版本号演进

| 项 | 变更内容 |
|---|---|
| 0.9.7 | 项目版本号由 0.9.6 升至 0.9.7（前端 `package.json`、后端 `app/__init__.py.__version__`、前端 `CHANGELOG.md` 锚点同步） |

## 待办闭环汇总（来源：项目待办事项.md 已关闭/已完成区，迁移合并至此作为唯一权威变更记录）

### P0 闭环

| 编号 | 变更内容 |
|---|---|
| Phase 6 | 社区搜索 GIN + tsvector 优化 + 事件总线跨实例（ADR-014）--搜索（posts+members）`search_vector`+GIN 索引+触发器；`websearch_to_tsquery`；集成测试 `test_phase6_search_gin.py`；事件总线在 `MULTI_INSTANCE=true` 时经 arq/Redis 广播，worker `on_startup` 注册订阅者；`docker-compose.yml` 新增 `redis`+`worker` 服务（详见 2026-08-01 Phase 6 执行记录） |
| PRR-P0-1 | 安全关键路径 Service 单元测试补齐--为 `password_reset_service`(12)、`verification_service`(12)、`totp_service`(13)、`oauth_service`(14)、`email_service`(12) 共生成 63 个测试，全部通过 |
| PRR-P0-2 | 数据库备份脚本--创建 `tools/scripts/backup_db.sh`：pg_dump 全量备份 + gzip 压缩 + 完整性校验 + 14 天自动清理 + `--restore` 恢复模式，RTO=4h/RPO=24h |
| PRR-P0-3 | SLO 与可观测性基线--创建 `tools/docs/BackDoc-SLO.md`：API 可用性 99%、p95<500ms、错误预算 432min/月、5 条告警规则（P0/P1）、日/周/月/季度运维巡检清单 |
| P0-后端CI | 创建 `CS-Web-Backend/.github/workflows/ci.yml`：Python 3.13 + pip install requirements-dev.lock + docker-compose.ci.yml（PG+Redis）+ Alembic round-trip + static checks（compileall/black/flake8/mypy）+ API contract freeze + pytest coverage + supply chain（pip_audit/cyclonedx/piplicenses）+ 制品上传。与 Jenkinsfile 对等，填补了后端无 GitHub Actions CI 的空白 |

### P1 闭环

| 编号 | 变更内容 |
|---|---|
| 38 | 根级 `data/` 占位--已建 `data/` + `README.md` + `.gitkeep` 固化挂载 |
| 60 | 文档对齐真实态--`RootDoc-FEArch.md`/`FrontDoc-01-Arch.md`/`项目演变历史-0.9.1.md`（原 FrontDoc-Evo.md 已并入）顶部加「当前进度/真实状态」；`项目演变历史.md` ADR-014 状态 待评估→已落地 |
| 前端 SQLite 清除 | `audit.ts` 写 SQLite（46）、方案 A/B 评估（47）、`shared/db` 删除（41/48/49）、邮件下沉（50）、`getRepositories` 工厂（52）、auth 子模块直连（53）--均随 BFF 纯转发 + 后端 PG 化已解决/不再需要 |
| 54/55 | CI 集成测试--`Jenkinsfile` + `docker-compose.ci.yml`（PG+Redis）已跑 pytest 集成测试 |
| 57 | 前端 CI 漏洞阻断--`ci.yml` 已 `pnpm audit --prod --audit-level=moderate` 强制阻断（G1） |
| 58 | 前端仓库清理--`.gitignore` 已覆盖 `test-results/`/`*.tsbuildinfo`/`data/`；无被跟踪 `.db` 文件 |
| 56 | 前端大页面拆分--`profile/page.tsx` 已拆为 hook + 子组件；`events/manage`、`community/compose` 路径已不存在 |
| 59 | 新手操作手册--根级 `docs/Onboarding.md` 已覆盖入职全流程（原 FrontDoc-Onboard.md 已合并删除） |
| 43 | pytest 依赖--`pyproject.toml` 已有 pytest 固化（G2） |
| 方案B-波次2/3 | 前端架构已为 BFF 薄转发，snake→camel 翻译在 `backend-client.ts` 完成；约 31 处局部 snake 变量降为低优风格项（移入 P2） |
| 39/44/45/51/52 | 方案 B 波次 6 联调文档、波次5 shared/db PG 引擎删除边界、pytest 依赖补装、前端 SQLite 残活引用、多库支持删除直连--均经核查随 BFF/PG 化已解决 |
| PRR-P1-4 | 密钥轮换 runbook--创建 `tools/docs/BackDoc-KeyRotation.md`：JWT 零停机轮换（`JWT_PREVIOUS_SECRET_KEYS`）、TOTP 轮换（需维护窗口 + 1.1 双密钥解密方案建议）、数据库密码轮换，含回滚步骤和记录模板 |
| PRR-P1-5 | Alembic downgrade 往返验证 + 多 head 修复--`22232b182a66c`（DROP 旧表）`down_revision` 从 `None` 改为 `22232b182a66b`，消除双 head；最新 5 个迁移 downgrade->upgrade 往返全部通过 |
| PRR-P1-6 | Redis + workers 校验--`config.py` 新增 `WORKERS` 字段和 `_warn_multi_worker_without_redis` 校验器：生产环境 workers>1 且无 REDIS_URL 时发出 WARNING |
| P1-Dependabot | 创建 `CS-Web-Backend/.github/dependabot.yml`（pip 每周一安全更新）、`CS-Web-Frontend/.github/dependabot.yml`（npm 每周一安全更新），开启依赖漏洞自动提 PR |
| P1-console.error | 清理 `src/modules/auth/ui/hooks/use-auth-form.ts` 中 4 处 `console.error`，替换为 `logger.error` 结构化日志；`auth-constants.ts:59` 为 fatal 启动错误（`process.exit(1)`），有意保留 |
| P1-健康检查 | 实现 `GET /health/events`：返回 `event_bus._subscribers` 各事件注册的监听器数量；`GET /health/security`：返回限流器后端类型（`using_redis`）、黑名单类型、AUTH_ENABLED/TOTP_ENCRYPTION_KEY 状态、DB/Redis 连接状态、多实例/WORKERS 配置。均无需鉴权，供运维探针使用 |

### P2 闭环（1.1 规划项，提前完成）

| 编号 | 变更内容 |
|---|---|
| PRR-P2-7 | CI 集成测试手动触发--前端 `.github/workflows/ci.yml` 新增 `workflow_dispatch` 手动触发器 + E2E 测试 job（Playwright + 报告上传）；后端 Jenkinsfile 已有完整集成测试（PG+Redis 容器 + 全量 pytest） |
| PRR-P2-8 | OTel 导出目标配置--`docker-compose.yml` 新增 `otel-collector` 服务（`--profile observability` 可选启动），创建 `tools/otel-collector/config.yaml`（OTLP 接收 + batch + memory_limiter + debug 导出，含 Prometheus/Jaeger 模板） |
| PRR-P2-9 | 前端引入 SWR--安装 `swr@2.5.0`，`layout.tsx` 添加全局 `SWRConfig`（请求去重+缓存），`use-auth.ts` 重构为 `useSWR`（消除多组件并发冗余 fetch），返回值接口不变，177 个前端测试通过 |
| PRR-P2-10 | 邮件服务 arq 队列--`tasks.py` 新增 `send_email_task`（注册到 TASKS），`email_service.py` 的 `send_mail` 在 `QUEUE_ENABLED=True` 时经 arq 异步投递，`worker.py` 配置 `max_tries=3` + `job_timeout=60s` |

### 方案已备（待评审执行）

| 编号 | 变更内容 |
|---|---|
| 51 | 后端旧表 DROP--可选清理迁移 `22232b182a66c_drop_legacy_community_community.py` 已备（DROP IF EXISTS + 备份提示 + 不可逆降级保护），待团队确认数据零丢失后执行 |

### 额外修复（审查过程中发现）

| 编号 | 变更内容 |
|---|---|
| PRR-FIX-1 | Alembic 多 head 修复--`22232b182a66c` 迁移 `down_revision` 从 `None` 改为 `22232b182a66b`，消除 `alembic upgrade head` 失败的阻塞问题（与 PRR-P1-5 同次修复） |
| PRR-FIX-2 | `pyproject.toml` 补充 `arq==0.28.0` 依赖--原仅在 `requirements.txt` 中，导致 `test_dependency_manifest` 测试失败 |

### 已关闭 / 已完成（归档，不再跟踪）

| 编号 | 关闭日期 | 结论 |
|---|---|---|
| 文档迁移-待办4 | 2026-08-05 | CODE_WIKI.md 从未创建，根 README 目录结构已改为指向实际存在的 `docs/`，根 `docs/README.md` 作为统一文档入口 |
| 子仓库文档漂移 | 2026-08-05 | FrontDoc-Ops.md 已重写为 BFF 视角、明确"去 SQLite/Litestream"，旧 LiteStream/drizzle 描述已清除，无需 submodule 维护者再处理 |
| 迁移脚本-手动1 | 2026-08-05 | 头像已复制到后端 `CS-Web-Backend/data/avatars/`（10 个 png/jpg 实测存在），与演变历史"静态资源迁移--avatars(5个) 已复制"一致 |
| 迁移脚本-手动2 | 2026-08-05 | 社区图片已复制到后端 `CS-Web-Backend/data/community-images/`（1 个 jpg 实测存在），与演变历史一致 |
| Phase 0-验证 | 2026-08-05 | 迁移验证已在 Linux/PG 环境执行；Phase 6 迁移脚本 migrate-sqlite-to-pg.mjs 已实现并跑通（19 张表入库、外键完整、类型转换正确） |
| 2FA 重加密+scrypt 懒升级 | 2026-08-05 | 演变历史记"2FA 迁移成功、scrypt 哈希直搬 + 懒升级 bcrypt"已完成 |
| 迁移验证 1-6 | 2026-08-05 | 逐表行数/外键/角色/序列/health/readyz/alembic 版本均通过（7 项验证全部通过） |
| 方案B-波次2-遗留 | 2026-08-05 | `community/community/follows/route.ts` 的 user 字段已输出 camelCase，原 snake_case 遗留已修复 |
| 方案B-波次4 | 2026-08-05 | `backend-client.ts` 已无 `toXxx` 翻译函数（代码核查 0 匹配），清理已完成 |
| 后端根目录-1/2/3 | 2026-08-05 | ARCHITECTURE.md/MIGRATION_PLAN.md/CONVENTIONS.md 已由后端根目录移入 docs/（根目录实测不存在这些文件） |
| 后端根目录-风险 | 2026-08-05 | 文档移动后全仓库搜索无硬引用残留，风险已消除 |

### P2 真伪核查归档

> 核查结论：下列 P2 条目经代码核查确认**已实现 / 已过时**，从待办事项移除，归档至此。

| 编号 | 核查结论 | 实现证据 |
|------|---------|---------|
| SI5 - TOTP_ENCRYPTION_KEY 启动断言 | ✅ 已实现 | `config.py:269-283` `@field_validator("TOTP_ENCRYPTION_KEY")` 已实现 fail-fast 校验（≥32 UTF-8 字节），未设置时启动即退出 |
| 例外1 - 限流/jti 防重放单进程内存 | ✅ 已实现 Redis 降级架构 | `DegradableRateLimiter`（`rate_limit/limiter.py`）优先 Redis，故障自动降级内存 + 半开重试；`DegradableBlacklist`（`security_blacklist.py`）同模式，jti 黑名单在 Redis 可用时写入 `SETEX`，Redis 故障时降级内存兜底 |
| 事件总线异步化 / ADR-014 | ✅ 已落地 | `events.py` `EventBus.emit` 在 `MULTI_INSTANCE=true` 时经 `dispatch_event_broadcast` arq 任务跨实例广播；单实例零开销（直接 in-process emit）（详见 2026-08-01 Phase 6 / ADR-014） |
| Phase 2-5 全部项（Drizzle schema/SQLite 类型/自增主键/日期/JSON/约束/索引/raw SQL 等） | ✅ 已过时 | 前端已降级为 BFF 薄转发，`src/modules/*/server/` 已全部删除（B1 闭环），后端使用 SQLAlchemy + Alembic + PostgreSQL，Drizzle/SQLite 迁移不再需要 |
| PG 双引擎（Part B Phase 2-5） | ✅ 已过时 | 前端不再有本地数据库引擎，后端仅 PG，无双引擎需求 |
| 2026-07-31-安全-Backlog-事件总线异步化 | ✅ 已落地 | 同上 ADR-014，已通过 arq/Redis 实现跨实例广播 |
