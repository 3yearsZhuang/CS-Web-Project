# 项目待办事项 · 优先级重排（2026-08-18）

> 本文档基于 `docs/项目待办事项.md`（SSOT）并结合 **`CS-Web-Backend` / `CS-Web-Frontend` 代码实况 + `CHANGELOG.md` 收口记录** 重新评估优先级。
> 重排方法：① 发布门槛（1.0.0 关键路径）分析 → ② RICE / 价值-努力矩阵 → ③ 风险与依赖排序 → ④ 清单过期审计（已闭环项降级/归档）。
> 原文仍是未完成任务权威跟踪（SSOT）；本文为**重排快照与同步建议**，请 SSOT owner 据"第四节"回填。

---

## 一、现状核查与清单过期审计

| 条目 | 原标注 | 代码实况核查（2026-08-18） | 处置 |
| --- | --- | --- | --- |
| **C-17** 上帝服务拆分 User/Auth | P1 🔴 | `user_service.py`=844、`auth_service.py`=775 行，**属实** | 维持高关注，但**降为 P2**（见第三节） |
| **C-16** service 直查下沉 repo | P1 🟡 | `self.db.execute` 实测 **24 处**（原写 28，略高估），仍待治理 | 维持，提至 P1 前置 |
| **C-18** repo 巨型文件拆分 | P2 🟢 | `community_repo.py`=718、`tools_repo.py`=621 行，**属实** | 维持 P2 |
| **R17** 审计严格落库 | P0（门槛#2） | 关键 admin 路径已迁移 `record_atomic`（**23 处**）；仅剩 **12 处** best-effort `record()`（auth 8 / password_reset 2 / event 1 / join 1） | **~85% 完成**，P0 缩为"核验剩余 12 处" |
| **AR-S2** 周期任务幂等 | P0（门槛#4） | `maintenance_tasks.py` **已不存在**；问题仍在且扩散：`exception_retention.py:68`、`token_gc.py:63`、`data_retention.py:98`、`view_count.py:145` 均用 `asyncio.create_task(_cleanup_loop)`，跨实例仍可能重复 | **问题有效、路径过期**，维持 P0 |
| **AR-S1 / ER-48** `/health/security` 暴露配置 | P1 / P2 | `app/main.py:214` 仍**无鉴权依赖**，返回 `AUTH_ENABLED`/`TOTP_ENCRYPTION_KEY_set`/`REDIS_URL` 状态 | 维持有效，提至 P1 安全快赢 |
| **CS-02** 前端测试盲区 | P1 🟢 | 前端已存在 **22 个 `*.test.ts*`** + `vitest.config.ts`，**非绿地** | **降级**：框架已落地，改为"扩覆盖" |
| **CS-03** `server.ts` 手写 .env / `;;` 笔误 | P1 🟢 | `server.ts:14-21` **已用 `dotenv`（`loadEnv({override:false})`）**；`user-menu.tsx` **无 `;;` 笔误**（仅 `assistant-chat.tsx` 合法 `for(;;)`） | **已闭环**，从 P1 移除、归档 |
| **D10** 1.0.0 功能边界拍板 | P0（门槛#6） | DM/Wiki/活动评价/相册 仍未拍板（搜 `wiki|gallery|album|dm` 仍 0 命中） | 维持 P0，且为**头号 gate** |
| C-15 / C-19 / AR-ACT1 等 | P1 | 声明与代码吻合（141 个 route.ts、40+ 模块手写 fetch、活动未入工作台） | 维持 P1，重排序 |

**结论**：原清单约 **2 项已闭环（CS-03）、2 项路径过期（AR-S2 / 部分引用）、2 项显著低估进度（R17 / CS-02）**。整体 P0→P2 的"标签"基本合理，但**排序与范围需按 1.0.0 关键路径重锚**。

---

## 二、重排后的优先级

### 🔴 P0 — 1.0.0 收口前须清零（关键路径，按阻断力排序）

| 序 | 条目 | 类型 | 重排理由 | 估算 |
| --- | --- | --- | --- | --- |
| **P0-1** | **D10** 功能边界拍板（DM/Wiki/活动评价/相册 纳入或显式延后，写 CHANGELOG） | 产品决策 / 门槛#6 | **唯一能解锁规划的 gate**。不定边界则 1.0.0 scope 永远模糊，AR-ACT1 等无法排期。需产品/负责人本周拍板。 | 0.5d 决策 + 文档 |
| **P0-2** | **AR-S2** 周期任务多实例幂等（改 arq cron 单点调度） | 工程 / 门槛#4 | 原文件已删但**问题扩散到 4+ 处**；当前若多实例部署，`_cleanup_loop` 会重复跑（token_gc / data_retention / view_count 等），属数据正确性问题。是"多实例无重复"承诺的真门槛。 | 2–3d |
| **P0-3** | **R17-closeout** 核验剩余 12 处 `audit.record()` 是否非关键 | 工程 / 门槛#2 | 关键 admin 路径已 `record_atomic`（23 处）。仅须逐处确认 12 处 best-effort 调用（登录/非敏感事件）确实允许丢失，补注释或迁移即可闭环。 | 0.5–1d |

> 门槛 #5（外部监控）**有意延后**：与 EX-1 一致，<200 用户风险接受，不阻塞 1.0.0（记入 P2 规模触发）。
> 门槛 #1（契约门禁）、管理员 2FA、ER-01 **已闭环**，不再列入。
> 门槛 #3 在原清单未枚举，建议 SSOT 补列其定义，避免 gate 表缺口。

### 🟡 P1 — 本波次（1.0.0 后首个重构波）收口，按价值/努力/风险排序

| 序 | 条目 | 价值×努力 | 风险 | 重排理由 |
| --- | --- | --- | --- | --- |
| **P1-1** | **AR-S1 / ER-48** `/health/security` 脱敏或加鉴权 | 高×低 | 低 | 安全快赢：无鉴权暴露配置状态。低风险先收，避免 1.0.0 发布后审计扣分。 |
| **P1-2** | **C-16** service 直查下沉 repo（24 处 → repository 唯一入口） | 中×中 | 中低 | **C-17 的前置依赖**：先统一查询入口，上帝服务拆分才安全。独立于大重构，可渐进。 |
| **P1-3** | **AR-M1 / AR-M2 / AR-D1 / AR-P1 / AR-X1** 门面与验证器收口 | 中×低 | 低-中 | 低风险维护性项，批量做、各带单测，降低后续重构摩擦。 |
| **P1-4** | **C-15** BFF 样板生成（openapi.baseline.json → route 骨架） | 高×中 | 低 | 消除 1000+ 行重复、新增 API 成本↓70%。先抽 `mapTo` 助手低风险起步。 |
| **P1-5** | **AR-ACT1** 活动合并入工作台（方案 A） | 中×中 | 中 | 功能交付，**依赖 D10 边界**。D10 拍板后立即做。 |
| **P1-6** | **C-19** 前端数据获取收敛（useApiRequest/SWR） | 高×中 | 中 | 价值高（40+ 模块手写 fetch）。**CS-02 框架已就位（22 测试+ vitest）**，可借势推进。 |
| **P1-7** | **CS-02（重定）** 前端单测覆盖扩展 | 中×中 | 低 | 由"绿地搭建"**重定为"扩覆盖"**：先 primitives + hooks，再动效组件。 |

### 🟢 P2 — 可暂缓（规模/远期触发 或 高风险重构后置）

| 序 | 条目 | 重排理由 |
| --- | --- | --- |
| **P2-1** | **C-17** 上帝服务拆分 User/Auth（844/775 行） | **风险最高且非 1.0.0 gate**。原建议即"0.9.9 发布后"，临发布前做高危重构会动摇稳定性——**明确后置到 1.0.0 之后、分阶段（每步可回滚）**。与 C-16、C-18 组成"查询层治理"专题。 |
| **P2-2** | **C-18** repo 巨型文件拆分（718/621 行） | 纯搬移、低风险，跟随 C-17 之后做。 |
| **P2-3** | **N-01 / N-12 / N-13** 密钥轮换登记 / 低风险增强 | 低优先，随文档维护顺手做。 |
| **P2-4** | **P2/L4+L5、Ops-SLI / R18、Ops-ErrorBudget、EX-1** | 规模触发（用户量逼出需求再立项），当前风险接受。 |
| **P2-5** | **Roadmap 功能缺口**：2.0 微服务、DM(M1)、Wiki(M2)、活动评价(M4)、相册(M5)、SSO(N2)、全局搜索(N1)、GDPR 导出删除(N3)、审核工具(N4)、L1/L2/L7/L8/L10 | 1.x 路线图，依赖 D10 边界与规模。 |
| **P2-6** | **Devdocs-Arch-3.3 / 文档迁移×3** | 依赖 docs 托管服务，远期。 |

### ⚪ P3 — 观察 / 可选（基本维持原样）

CodeGov-F1（CJK 字体）、CS-07/CS-08、EmptyState、前端评审不做项、i18n 环境报错、DocGov-D 例外、CodeGov-B2、ER-58/ER-59、R3/R4 —— 维持观察，无变化。

---

## 三、1.0.0 关键路径与冲刺序列

```
Sprint A（发布前清零）
  ├─ D10       拍板功能边界 ──────────────► 解锁 AR-ACT1 排期
  ├─ AR-S2     周期任务改 arq cron 单点 ──► 满足"多实例无重复"承诺
  └─ R17-close 核验 12 处 record() 非关键 ► 闭环"管理员审计不丢"

Sprint B（1.0.0 后首个重构波，按依赖推进）
  ├─ AR-S1 安全快赢（脱敏/health/security）
  ├─ C-16 service→repo 下沉（C-17 前置）
  ├─ AR-M*/D1/P1/X1 门面与验证器收口（批量）
  └─ C-15 BFF 样板生成（降重复）

Sprint C（功能与前端）
  ├─ AR-ACT1 活动入工作台（D10 后）
  ├─ C-19 前端 fetch 收敛 + CS-02 扩覆盖
  └─ 视容量插入 C-17 起步（若本波仍有余量，仅做 User 单侧试点）

后置（1.0.0 之后专题）
  └─ C-17 上帝服务拆分 → C-18 repo 拆分（"查询层治理"专题，分阶段可回滚）
```

**依赖链关键约束**：C-16 必须在 C-17 之前；AR-ACT1 必须在 D10 之后；C-17/C-18 必须等 1.0.0 发布稳定后再启动。

---

## 四、对 SSOT（`项目待办事项.md`）的同步建议

请 SSOT owner 据本次核查做以下最小改动（避免破坏 SSOT 格式）：

1. **移除已闭环**：`CS-03` 从 P1 移出，按格式归档根 `CHANGELOG.md`（dotenv 已落地、`;;` 笔误不存在）。
2. **重定 CS-02**：P1 描述由"前端测试盲区（0 命中）"改为"前端单测扩覆盖（已 22 文件 + vitest 就位）"。
3. **修正 AR-S2 路径**：描述中 `maintenance_tasks.py:31` 改为"周期清理循环现分布于 `exception_retention/token_gc/data_retention/view_count` 4 处 `asyncio.create_task`"，问题有效、路径过期。
4. **R17 缩范围**：P0 描述改为"核验剩余 12 处 `audit.record()`（auth 8 / password_reset 2 / event 1 / join 1）是否非关键"。
5. **C-16 数字**：28 处改为实测 24 处。
6. **C-17 降级提示**：在 P1 项末补"（风险最高、非 gate，建议 1.0.0 后做）"或移至 P2。
7. **补门槛 #3**：在 P0 判定依据中补列 #3 定义，闭合 gate 表缺口。

> 以上为事实性同步；编号、关联、收益/风险字段保持不变即可。
