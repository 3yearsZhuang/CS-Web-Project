# 项目待办事项 · 优先级重排（2026-08-18）

> 本文档为**未完成任务唯一权威跟踪（SSOT）**（2026-08-18 起：原 `docs/项目待办事项.md` 已并入本文并删除，未迁移条目见「附录 A」）。评估结合 **`CS-Web-Backend` / `CS-Web-Frontend` 代码实况 + `CHANGELOG.md` 收口记录**。
> 重排方法：① 发布门槛（1.0.0 关键路径）分析 → ② RICE / 价值-努力矩阵 → ③ 风险与依赖排序 → ④ 清单过期审计（已闭环项降级/归档）。
> 已收口项统一归档根 `CHANGELOG.md`，正文不留 ✅；格式沿用原 SSOT：`状态 编号｜描述:正文`，补充字段内联 `` `字段:值` ``。

---

## 一、现状核查与清单过期审计

| 条目 | 原标注 | 代码实况核查（2026-08-18） | 处置 |
| --- | --- | --- | --- |
| **C-17** 上帝服务拆分 User/Auth | P1 🔴 | `user_service.py`=844、`auth_service.py`=775 行，**属实** | 维持高关注，但**降为 P2**（见第三节） |
| **C-16** service 直查下沉 repo | P1 🟡 | `self.db.execute` 实测 **24 处**（原写 28，略高估），1 处真代码味已治（user_service 级联 DELETE 表名 f-string 插值→字面量），剩余 23 处参数化 Core 查询骑乘 C-17 | 部分治理（2026-08-18 选定方向 C） |
| **C-18** repo 巨型文件拆分 | P2 🟢 | `community_repo.py`=718、`tools_repo.py`=621 行，**属实** | 维持 P2 |
| **R17** 审计严格落库 | P0（门槛#2） | 关键 admin 路径已迁移 `record_atomic`（**23 处**）+ 本次 3 处管理员审计（password_reset.approve/reject、join.approve/reject）迁 `record_atomic`、9 处非关键（auth×8 / event._audit）加注释 | **已闭环**（2026-08-18：3 处管理员原子化 + 9 处注释，见 CHANGELOG [Unreleased]）；`user_service._audit_admin`（P2-7）已于 2026-08-18 同方案闭环 |
| **AR-S2** 周期任务幂等 | P0（门槛#4） | 轻量闭环已落地（三处 GC/保留期改启动兜底跑一次 `_purge_once` + advisory lock；`view_count` 保留周期循环用 Redis `getset` 单点）；方案 B arq cron 单点周期调度已落地（`WorkerSettings.cron_jobs`：token_gc 每小时 / data_retention 03:00 / exception_retention 03:30），web 启动兜底作 cold-start 兜底 | **已闭环**（2026-08-18：轻量闭环 + 方案 B arq cron，见 CHANGELOG [Unreleased]） |
| **AR-S1 / ER-48** `/health/security` 暴露配置 | P1 / P2 | `app/main.py:214` 仍**无鉴权依赖**，返回 `AUTH_ENABLED`/`TOTP_ENCRYPTION_KEY_set`/`REDIS_URL` 状态 | **已闭环**（2026-08-18：选择 A1 脱敏方案，移除 `auth` 配置姿态块，保留无鉴权运维探针 + 补充脱敏测试，见 CHANGELOG [Unreleased]） |
| **CS-02** 前端测试盲区 | P1 🟢 | 前端已存在 **22 个 `*.test.ts*`** + `vitest.config.ts`，**非绿地** | **降级**：框架已落地，改为"扩覆盖" |
| **CS-03** `server.ts` 手写 .env / `;;` 笔误 | P1 🟢 | `server.ts:14-21` **已用 `dotenv`（`loadEnv({override:false})`）**；`user-menu.tsx` **无 `;;` 笔误**（仅 `assistant-chat.tsx` 合法 `for(;;)`） | **已闭环**，从 P1 移除、归档 |
| **D10** 1.0.0 功能边界拍板 | P0（门槛#6） | **已拍板（2026-08-18 SSOT 更新）**：Wiki/活动评价纳入 1.0 规划；DM/相册延后 P1（已写 CHANGELOG [Unreleased]） | 由「拍板」转为**执行跟踪**（Wiki 新模块规划推进、活动评价随活动模块补） |
| C-15 / C-19 / AR-ACT1 等 | P1 | 声明与代码吻合（141 个 route.ts、40+ 模块手写 fetch、活动未入工作台） | 维持 P1，重排序 |

**结论**：原清单约 **3 项已闭环（CS-03、R17、AR-S2）**、1 项路径过期（部分引用，随 AR-S2 重锚消解）、2 项显著低估进度（R17 / CS-02，均已重估）**。整体 P0→P2 的"标签"基本合理，但**排序与范围需按 1.0.0 关键路径重锚**。

---

## 二、重排后的优先级

### 🔴 P0 — 1.0.0 收口前须清零（关键路径，按阻断力排序）

| 序 | 条目 | 类型 | 重排理由 | 估算 |
| --- | --- | --- | --- | --- |
| **P0-1** | **D10** 功能边界（执行跟踪：Wiki/活动评价纳入 1.0、DM/相册延后 P1，已拍板写 CHANGELOG） | 产品决策 / 门槛#6 | **唯一解锁规划的 gate**——已拍板（2026-08-18，见 CHANGELOG [Unreleased]），转为**执行跟踪**：Wiki 新模块规划推进、活动评价随活动模块补，为 AR-ACT1 等解锁排期。 | 0.5d 决策 + 文档 |
| **P0-2** | **AR-S2** 周期任务多实例幂等（方案 B arq cron 单点调度） | 工程 / 门槛#4 | **已闭环**（2026-08-18）：轻量闭环消除空转循环 + arq cron 单点周期调度落地（`WorkerSettings.cron_jobs`），门槛#4「多实例无重复」满足；web 启动兜底作 cold-start 兜底。见 CHANGELOG [Unreleased]。 | 0.5–1d（已完成） |
| **P0-3** | **R17-closeout** 核验剩余 12 处 `audit.record()` 是否非关键 | 工程 / 门槛#2 | **已闭环**（2026-08-18）：3 处管理员审计（password_reset.approve/reject、join.approve/reject）迁 `record_atomic`、9 处非关键（auth×8 / event._audit）加注释；`auth_service` 构造函数同步注入 `self.db` 修复既有 `user.create` 原子审计；`user_service._audit_admin` 超原 12 处范围，记 P2-7 后续。见 CHANGELOG [Unreleased]。 | 0.5–1d（已完成） |
| **P0-4** | **门槛#3 端到端验收闭环（E2E 发布前必过）** | 工程 / 门槛#3 | 真实 docker-compose 全栈形态下核心用户旅程（注册/登录/2FA/社区发帖/活动报名/管理操作/审计落库）E2E 全绿，1.0.0 发布前必过（非仅 nightly）。与 #1 契约门禁互补：契约对得上 ≠ 端到端跑得通。已于 2026-08-19 闭环：根 `.github/workflows/e2e.yml` 加 `push: branches: [main]` 触发，合并到 main 即跑全栈 E2E，保证默认分支「可发布绿」（保留 nightly + 手动），对应门槛#3「E2E 发布前必过」。 | 0.5–1d（已完成） |

> 门槛 #1（契约门禁）、管理员 2FA、ER-01 **已闭环**，不再列入。
> 门槛 #2 管理员审计不丢（R17）**已闭环**（见 P0-3）。
> 门槛 #3 **端到端验收闭环（E2E 发布前必过）｜2026-08-19 补列定义并闭环**：真实 docker-compose 全栈部署形态下核心用户旅程（注册/登录/2FA/社区发帖/活动报名/管理操作/审计落库）E2E 全绿，作为 1.0.0 发布前必过（而非仅 nightly）；与 #1 契约门禁互补（契约对得上 ≠ 端到端跑得通）。**已闭环**（2026-08-19：根 `.github/workflows/e2e.yml` 加 `push: branches: [main]` 触发，合并到 main 即跑全栈 E2E，保证默认分支「可发布绿」；保留 nightly + 手动），对应执行项 P0-4。
> 门槛 #4 多实例无重复（AR-S2）**已闭环**（见 P0-2）。
> 门槛 #5（外部监控）**有意延后**：与 EX-1 一致，<200 用户风险接受，不阻塞 1.0.0（记入 P2 规模触发）。
> 门槛 #6 D10 功能边界**已拍板/执行跟踪**（见 P0-1）。

### 🟡 P1 — 本波次（1.0.0 后首个重构波）收口，按价值/努力/风险排序

| 序 | 条目 | 价值×努力 | 风险 | 重排理由 |
| --- | --- | --- | --- | --- |
| **P1-1** | **AR-S1 / ER-48** `/health/security` 脱敏或加鉴权 | 高×低 | 低 | 安全快赢：无鉴权暴露配置状态。低风险先收，避免 1.0.0 发布后审计扣分。 | **已闭环**（2026-08-18：选 A1 脱敏，删 `auth` 块 + 补断言测试，见 CHANGELOG [Unreleased]） |
| **P1-2** | **C-16** service 直查下沉 repo（24 处；方向 C：仅治真代码味，主体骑乘 C-17） | 中×中 | 中低 | **C-17 的前置依赖**：先统一查询入口，上帝服务拆分才安全。1.0.0 前只治真代码味（f-string 表名插值），其余骑乘 C-17 拆分时下沉 repo，避免重复重构（2026-08-18 选定方向 C）。 |
| **P1-3** | **门面与验证器收口**（原 AR-M1…X1 定义已随初版 SSOT 删除，2026-08-18 按代码重释） | 中×低 | 低-中 | 低风险维护性项。代码重释范围：① 验证器死代码清理（已完成 `validate_email`）② email 校验一致性审查（已完成：输入 `EmailStr`+长度 / 输出 `UserOut.email:str` 仅长度属有意设计、宽松约定保留；新增 `tools/tests/schemas/test_email_validation.py` 8 passed）③ 门面 facade 勘察（已完成：DI 门面主体健康，32 工厂；发现泄漏 TOTPService/AuxilioService/WorkbenchService/ContributionService/community 直查）④ facade 收口（已完成：A workbench 补 `get_workbench_service`+`get_contribution_service` 并路由 3 endpoint；B auxilio 注入 `get_auxilio_service` 收敛 3 处内联；方向 B 首点 `get_totp_service`+`feature_visibility.py`；router 层已无 `TOTPService(`/`AuxilioService(`/`WorkbenchService(`/`ContributionService(` 内联。C community 两处裸查询 endpoint 无 service 可代 → 判定延期，归 C-16/C-17）。P1-3 实质闭环（剩余属更大重构范畴）。各带单测，降低后续重构摩擦。 |
| **P1-4** | **C-15** BFF 样板生成（openapi.baseline.json → route 骨架） | 高×中 | 低 | 消除 1000+ 行重复、新增 API 成本↓70%。**勘察纠正**：类型已由 `gen:api-types`（`openapi-typescript`）自动化，缺口在 140 个 `route.ts` 运行骨架样板；`backend-client.ts` 已有 ~13 个 `to*` schema 翻译助手，故只抽**通用 handler 原语**（方向 A 进行中：`bodyOrEmpty`/`arrayFrom`/`okJson`/`errJson`/`readJsonBody` 已加，已迁移 component-registry/overview/favorites/follows 共 4 个 route.ts，行为不变，ts-check 无新增/基线 10、bff-boundary 通过）。剩余 ~137 个待迁移或直接上生成器。后续：手工迁移更多 route.ts → 上 route 骨架生成器（B 全量 / C 增量）。

  **C-15 生成器已建成（2026-08-18）**：`CS-Web-Frontend/tools/scripts/fe/gen/bff-routes.mjs` 解析 `openapi.baseline.json`（172 路径/223 操作），按「剥 `/api/v1` + `{param}`→`[param]`」产出使用新原语（`proxyBackend`/`bodyOrEmpty`/`arrayFrom`/`okJson`/`errJson`/`readJsonBody`）的 route 骨架；动态参数用 `Promise<{...}>` 签名；请求体按 schema 自动生成 camel→snake 映射；GET 转发原始 query。安全定位：默认 dry-run 到 `.bff-scaffold/`，绝不触碰 `src`；自动对账扫描现有 140 手写 route 的反向代理路径，标注 COVERED（含 comments↔replies / posts↔topics 目录重映射，靠精确归一匹配）/ NEW。**结果（去重前）：109 COVERED / 63 NEW；（修复 doubled-path 后）123 COVERED / 49 NEW**，63 个 NEW 骨架已落草稿 + 产出 `manifest.json` + `RECONCILE.md`。校验：内部 `createSourceFile` 语法检查 63/0，并用项目真实 tsc（scoped tsconfig）交叉验证 0 报错。期间修复生成器 `paramsType` 模板漏写 `Promise<{...}>` 闭合 `>` 的真实 bug（已修，校验通过）。**已 --write 交付（2026-08-18）**：新建 43 个单段骨架到 `src/app/api`（6 个已存在 route 命中 `existsSync` 跳过、0 覆盖；写模式漏 `mkdirSync` 父目录致首跑 ENOENT 崩溃已修）；`pnpm ts-check` 10 基线零新增、`pnpm check:bff-boundary` 通过。另系统性修复 doubled `/community` 段 BFF 代理 bug（16 文件/31 处去重，series 合法例外），见 CHANGELOG [Unreleased] Fixed。**覆盖率检测复盘修复**：`normalizeProxyPath` 未剔除 GET 代理模板末尾 `${url.search}` 致 15 个已交付 GET 骨架/手写路由误判 NEW（22→7），已修；最终对账 165 COVERED / 7 NEW（172 操作后），剩余 7 个均为预存在手写路由（6 个 raw `fetch` 直连、扫描器仅识别 `proxyBackend` 的 `path:` 故盲区；1 个 `auth/login` 手写代理到 `/auth/login-email` 属不同端点），均已被 `existsSync` 跳过、非 C-15 缺口。 |
| **P1-5** | **AR-ACT1** 活动合并入工作台（方案 A） | 中×中 | 中 | 功能交付，**依赖 D10 边界**。D10 拍板后立即做。 |
| **P1-6** | **C-19** 前端数据获取收敛（useApiRequest/SWR） | 高×中 | 中 | 价值高（40+ 模块手写 fetch）。**CS-02 框架已就位（22 测试+ vitest）**，可借势推进。**（收敛进展 2026-08-18：① 样板 `useFollow` 抽出（CP#1）；② 共享客户端原语 `useApiRequest` 已建成 `src/shared/hooks/use-api-request.ts`：`apiRequest<T>`+`useApiRequest<T>`，与后端 `server-only` `backend-client.ts` 互补；③ 批量迁移 `report-button`→`useReport`、`announcement-banner`→`useAnnouncements`（CP#3-②）；④ 三处 hook（useFollow/useReport/useAnnouncements）裸 fetch 全部收敛到 `apiRequest`（CP#4-① 收尾），ts-check 10 基线零新增、use-follow 单测 6/6；`useApiRequest` 原语已可用，后续 40+ 模块直连 fetch 可批量接入。迁移启动（2026-08-18 晚）：用户选**架构收敛·抽 hook**策略；建 10 域任务板（#15~#24，推荐序列 notification-bell→community-*→admin-*→tools/*→auth/profile→events→about→workbench→shared-hooks→misc，共 61 文件）。notification-bell 已完成（抽 `useNotificationBell` hook，4 处 fetch 收敛 `apiRequest`，含 markRead/markAllRead 乐观回滚，ts-check 10 基线零新增）；**community-* 域全域完成（14 文件：manager 类 6 + 非 manager 8，含 2 抽 hook/12 就地换 `apiRequest`；`community/ui` 与 `app/community` 两目录裸 `fetch(` 归零，ts-check 10 基线零新增）；共享原语 `use-api-request.ts` 增强 `extractError` 读 message||error + 支持 FormData/Blob/URLSearchParams raw body**。用户于 2026-08-18 末指令「继续完成全部的收敛任务」→ 切换**批量收敛模式**（按域连续推进、每文件保持 10 基线零新增、文档按域/里程碑批量更新、不再逐文件暂停）。**全域收敛完成（2026-08-19）**：#17~#24 全部域已收口——admin-*（14，含修复 #17 失败代理误改坏的 `use-user-list.ts` 的 `r` 变量重名 TDZ 错误）、tools/*（11，含 `component-registry-store` 5 个基线错误零新增）、auth-profile（7，SWR 文件 `use-auth`/`use-auth-form`/`session-manager` 保留不迁）、events（2）、about（1）、workbench（5，仅 `loadConversations`/`openConversation` 换、assistant-chat SSE 保留）、shared-hooks（3）、misc（3）。全局客户端裸 `fetch(` 仅余排除项（BFF `route.ts` / `server-only` `backend-client.ts` / 原语自身 / SWR 基础设施 `swr-provider`·`use-auth` / `assistant-chat` SSE 流式），ts-check 恒为 10 基线零新增（navbar×2 / component-registry-store×5 / use-auth.test×2 / use-feature-visibility.test×1）。策略 B（组件内联 fetch 抽专用 use-* hook；已是 use-* 的就地换 apiRequest）全部落地。是否给 useReport/useAnnouncements 补测试待定）** |
| **P1-7** | **CS-02（重定）** 前端单测覆盖扩展 | 中×中 | 低 | 由"绿地搭建"**重定为"扩覆盖"**：先 primitives + hooks，再动效组件。 |

### 🟢 P2 — 可暂缓（规模/远期触发 或 高风险重构后置）

| 序 | 条目 | 重排理由 |
| --- | --- | --- |
| **P2-1** | **C-17** 上帝服务拆分 User/Auth（844/775 行） | **风险最高且非 1.0.0 gate**。原建议即"0.9.9 发布后"，临发布前做高危重构会动摇稳定性——**明确后置到 1.0.0 之后、分阶段（每步可回滚）**。与 C-16、C-18 组成"查询层治理"专题。 |
| **P2-2** | **C-18** repo 巨型文件拆分（718/621 行） | 纯搬移、低风险，跟随 C-17 之后做。 |
| **P2-3** | **N-01 / N-12 / N-13** 密钥轮换登记 / 低风险增强 | 低优先，随文档维护顺手做。 |
| **P2-4** | **P2/L4+L5、Ops-SLI / R18、Ops-ErrorBudget、EX-1** | 规模触发（用户量逼出需求再立项），当前风险接受。 |
| **P2-5** | **Roadmap 功能缺口**：2.0 微服务、DM(M1)、Wiki(M2)、活动评价(M4)、相册(M5)、SSO(N2)、全局搜索(N1)、GDPR 导出删除(N3)、审核工具(N4)、L1/L2/L7/L8/L10 | 1.x 路线图，依赖 D10 边界与规模。 |
| **P2-6** | **Devdocs-Arch-3.3 / 文档迁移×3** | 依赖 docs 托管服务，远期。 |
| **P2-7** | **`user_service._audit_admin` 管理员审计原子化** | R17 范畴延伸（超出原 12 处）：`user_service.py` `_audit_admin` 已由 best-effort `AuditService().record()` 改为 `record_atomic`，构造函数注入 `AuditService(self.db)`；4 处管理员操作（update / enable·disable / reset_password / delete）移除提前 `commit()` 同事务原子提交（审计失败整体回滚）。门槛#2「管理员审计不丢」在 `user_service` 侧亦闭环，与 auth_service 一致。 | **已闭环**（2026-08-18，见 CHANGELOG [Unreleased]） |

### ⚪ P3 — 观察 / 可选（基本维持原样）

CodeGov-F1（CJK 字体）、CS-07/CS-08、EmptyState、前端评审不做项、i18n 环境报错、DocGov-D 例外、CodeGov-B2、ER-58/ER-59、R3/R4 —— 维持观察，无变化。

---

## 三、1.0.0 关键路径与冲刺序列

```
Sprint A（发布前清零）
  ├─ D10       边界执行跟踪（已拍板：Wiki/活动评价入 1.0、DM/相册延后）► 解锁 AR-ACT1 排期
  ├─ AR-S2     周期任务改 arq cron 单点 ──► 满足"多实例无重复"承诺（2026-08-18 已闭环）
  └─ R17-close 核验 12 处 record() 非关键 ► 闭环"管理员审计不丢"（2026-08-18 已闭环）

Sprint B（1.0.0 后首个重构波，按依赖推进）
  ├─ AR-S1 安全快赢（脱敏/health/security，2026-08-18 已闭环）
  ├─ C-16 service→repo 下沉（C-17 前置）
  ├─ AR-M*/D1/P1/X1 门面与验证器收口（批量）
  └─ C-15 BFF 样板生成（降重复）

Sprint C（功能与前端）
  ├─ AR-ACT1 活动入工作台（D10 后）
  ├─ C-19 前端 fetch 收敛 + CS-02 扩覆盖
  └─ 视容量插入 C-17 起步（若本波仍有余量，仅做 User 单侧试点）

后置（1.0.0 之后专题）
  └─ C-17 上帝服务拆分 → C-18 repo 拆分（"查询层治理"专题，分阶段可回滚）
```

**依赖链关键约束**：C-16 必须在 C-17 之前；AR-ACT1 必须在 D10 之后；C-17/C-18 必须等 1.0.0 发布稳定后再启动。

---

## 四、SSOT 合并与同步建议落实（2026-08-18）

> 原 `docs/项目待办事项.md` 已于 2026-08-18 并入本文（未迁移条目收纳于附录 A）并删除。本节为合并时对原 SSOT 的事实性同步项，均已在本文落实：

1. **移除已闭环**：`CS-03` 从 P1 移出（dotenv 已落地、`;;` 笔误不存在），本文核查表已标注「已闭环」。
2. **重定 CS-02**：P1 描述重定为"前端单测扩覆盖（已 22 文件 + vitest 就位）"，见 P1-7。
3. **修正 AR-S2 路径**：`maintenance_tasks.py` 已删，问题分布于 `exception_retention/token_gc/data_retention/view_count` 4 处，见 P0-2。
4. **R17 缩范围**：P0 描述为"核验剩余 12 处 `audit.record()`（auth 8 / password_reset 2 / event 1 / join 1）"，见 P0-3。
5. **C-16 数字**：28 处改为实测 24 处，见核查表与 P1-2。
6. **C-17 降级提示**：已移至 P2-1（风险最高、非 gate，1.0.0 后做）。
7. **补门槛 #3（2026-08-19 已落实）**：门槛 #3 已补列定义为「端到端验收闭环（E2E 发布前必过）」，见 P0 区块注记与 P0-4；状态为**未闭环**，对应执行项 P0-4（须在发布前补发布前 E2E 必过门禁 + 归档验收证据）。

> 编号、关联、收益/风险字段均与原 SSOT 保持一致。

---

## 附录 A：原 SSOT 未迁移条目收纳（2026-08-18）

> 原 `docs/项目待办事项.md`（2026-08-18 并入本文后删除）中未被本文「二、重排后的优先级」单列或未收录的条目，按原优先级、原格式收纳于此，保持跟踪不丢失。格式：`状态 编号｜描述:正文` + 内联 `` `字段:值` ``。

### A.1 P2 可暂缓（波次4 次级项 + 远期新功能 + 工程低危项）

- **AR-M3 小 M 重构**：`validate_project_session_exists` 改查 `user_purpose` 表（替代 project_sessions 旁路）；`actions/community.py` 减 `time.sleep` 依赖；`deps.py` 改依赖注入。
- **N-12 / N-13 / F-9 低风险增强**（N-12/N-13 已在正文 P2-3，此处保留 F-9 完整记录）：① 前端 `AGENTS.md` 补「外链/凭证/绝对路径」红线段 ② `tag_and_release.sh` 补 changelog 自动撰写 ③ 本地 `alembic heads` 实测复核，修正文档 heads 描述（文档冲突审查 F-9 标注「未做」）。
- **2026-08-18-P2/工作台-LeetCode 热力图**：外部数据源 `leetcode.cn/graphql` 脆弱（端点易变/防爬），需熔断降级 + 缓存兜底。
  `来源:工作台改造方案 §9 待办/§8 P4(2026-08-18 自 docs/plans/ 归档)` `建议:独立 service 隔离 + 指数退避 + 失败明示「数据非实时」`
- **2026-08-18-P2/可见性-定时上下线**：功能可见性决策点 D 留作后续迭代（定时对某类用户上下线模块）。
  `来源:功能模块可见性管理方案 §7 决策 D(2026-08-18 自 docs/plans/ 归档)`
- **ER-48~ER-57 工程审查低危项（🟢）**（ER-48 已在正文 P1-1 合并跟踪，2026-08-18 随 P1-1 闭环）：
  - **ER-49 ERR｜通知副作用失败仅 debug**：关键通知升 error/warning（关联 R17）。
  - **ER-50 PRF｜FTS 配置名拼接**、**ER-51 COD｜表名 f-string 拼接（含 CodeGov-B3）**：改白名单映射查表（均无注入风险，属写法收敛）。
  - **ER-52 DEP｜nodemailer 放 dependencies**：`CS-Web-Frontend/package.json:32`，移 server-only 服务端专用依赖。
  - **ER-53 ARC｜前端 scrypt 边界**：已由 check-bff-boundary CI 兜底（AL-1 落地），**闭环**。
  - **ER-54 CFG｜非密钥默认值写死**、**ER-57 OPS｜缺全局日志脱敏**：纵深防御项。

### A.2 P3 观察 / 可选（2026-08-18 新增）

- **2026-08-18-P3/工作台-Phase 4 纵深**：每周复盘卡（依赖番茄钟二期/统计模块数据积累）、系统状态角标（仅管理员）、浏览器 Notification 可选增强、数据导出（范围未展开）。
  `来源:工作台改造方案 §5 Phase 4/§8 P4(2026-08-18 自 docs/plans/ 归档)` `优先级:按需/可选`

### A.3 状态修正（合并时同步原 SSOT 最新结论）

- **D10**：原 SSOT 已拍板——Wiki/活动评价纳入 1.0 规划，DM/相册延后 P1（已写 CHANGELOG [Unreleased]）。对应正文核查表/P0-1 已更新为「执行跟踪」。
- **AR-S2**：原 SSOT 注明「轻量闭环已落地（见 CHANGELOG [Unreleased]）：`_purge_once` 内 `pg_try_advisory_xact_lock` / Redis `getset` 保证单点；后续 P2 方案 B 改 arq cron 单点调度补回周期清理」。**方案 B 已于 2026-08-18 落地**（`WorkerSettings.cron_jobs` 注册三 cron 任务），正文 P0-2 现已标「已闭环」；原"问题扩散"描述为闭环前状态，已失效。
