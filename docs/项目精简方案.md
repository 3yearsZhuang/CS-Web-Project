# 项目精简方案（冗余清理 + 重复共享化）

> 状态：**全部执行完毕 / 决策收口（2026-08-19）**：Phase 1 ✅、Phase 2 ✅、Phase 3 的 B3/B4 ✅、B5 核实无需动作 ✅、B1 有意延后 ⏸（见 §4.1 B1 行）。
> 落地过程中对原方案前提做了三处纠错（tw-animate-css 引用、D3 重复目标、S1 复用不可行）+ 一处 B2 形态修正（模块级 helper），并修复 c146d38 重命名回归，见 §3.2 / §4.1 / §4.3 / §5.1 备注（~~删除线~~ 为原方案内容，保留对照）。
> 日期：2026-08-19
> 范围：根仓（`docs/`、`scripts/`、`Makefile`、`.github/`、`CHANGELOG.md`）、`CS-Web-Backend`、`CS-Web-Frontend`
> 依据：全仓 AST 导入引用图 + 代码模式匹配 + 文档对照审计（2026-08-19）

---

## 1. 背景与目标

项目已发布 1.0.0，进入维护期。本次审计目标：

1. **删除**确定多余、无引用、已过时的代码与文件；
2. **抽取**重复代码块为共享实现，消除多份维护；
3. **治理**文档与配置冗余，修复门禁缺陷；
4. 所有删除项经**本地启动验证**确认零影响后才算完成。

**审计方法**：后端 221 模块 AST 导入图；前端 462 源文件引用计数；文档逐文件对照 + 治理规则（`DocGovernance.md`）合规检查。

---

## 2. 总览结论

| 维度 | 结论 |
|---|---|
| 后端死代码 | 无整文件孤岛（`worker.py`/`rbac_init.py` 等经 arq/生命周期注册，非死代码）；函数级建议后续跑 `vulture` 补充 |
| 后端重复 | 事务提交散落 **141 处**、分页 `offset+limit` **8 个 repo**、分页响应模型 2 套不一致、`utcnow()` 绕过集中工具 4 处 |
| 前端死代码 | **4 个文件 0 引用**（含 2 个大组件疑似未挂载）；依赖 2 个疑似未用 |
| 前端重复 | 日期格式化 2 处重复声明；Modal 背板/ESC 逻辑分散 4+ 处 |
| 文档冗余 | `api-reference.md` 手写漂移、待办文件已闭环条目滞留、公共组件报告 §2.1 整段重复、CHANGELOG 格式违规 |
| 配置/CI 缺陷 | `Makefile check-contract` 静默重写基线（门禁失效）、`e2e.yml` 与 `ci.yml` 重复检查 |

---

## 3. 可删除项清单（代码 / 文件）

### 3.1 前端孤立文件（确定可删）

| 文件 | 行数 | 证据 | 建议 |
|---|---|---|---|
| `CS-Web-Frontend/src/modules/admin/ui/admin-notifications-panel.tsx` | 284 | 全仓 0 引用；通知广播历史已由 `broadcast-history-panel.tsx` 接管 | **已删除**（Phase 1，FE `2f69bde`） |
| `CS-Web-Frontend/src/modules/admin/ui/admin-events-settings.tsx` | 361 | 全仓 0 引用；注释声称嵌入 `admin-events-panel` 但实际未 import | ~~删除或接入~~ → **已接入**（§6 决策 B：挂载到 `admin-events-panel.tsx`，工具栏新增 Settings 入口） |
| `CS-Web-Frontend/src/shared/app-error.ts` | 32 | 全仓 0 引用；前端 `AppError` 类无调用方 | **已删除**（Phase 1，FE `2f69bde`） |
| `CS-Web-Frontend/src/shared/security/tenant-context.ts` | 33 | 全仓 0 引用；多租户占位接口（单租户 no-op） | **已删除**（Phase 1，FE `2f69bde`） |

> 注：其余 26 个"0 引用"候选均为 `*.test.ts(x)`（vitest 测试）与 App Router 约定文件（`robots.ts` 等），**非死代码，保留**。

### 3.2 前端依赖冗余

| 依赖 | 证据（执行复核） | 结论（Phase 2 #7 已执行） |
|---|---|---|
| `tw-animate-css` | ~~`src/` 与 `tailwind.config` 均未引用~~ → **实际被 `src/app/globals.css:27` `@import 'tw-animate-css';` 引用**（原审计漏看） | **保留**（移除会破坏 CSS 动画） |
| `pino-pretty` | 仅开发期日志美化（`src/shared/logger.ts` 以 `NODE_ENV !== 'production'` 门控，生产走 NDJSON） | **已移入 `devDependencies`**（`package.json` + `pnpm-lock.yaml` 已同步） |

### 3.3 后端卫生项

| 项 | 证据 | 建议 |
|---|---|---|
| `CS-Web-Backend/.gitignore` 缺 `uploads/` | `data/` 已忽略但 `uploads/` 未声明，运行时上传文件易误入库 | 追加 `uploads/` |
| 9 个文件带 UTF-8 BOM | `admin_events.py`、`event_repo.py`、`tools_repo.py`、`auxilio_service.py`、`component_registry_service.py`、`event_service.py`、`exam_service.py`、`resource_service.py`、`task_service.py` | 批量去 BOM（UTF-8 无签名） |

### 3.4 根仓临时文件

`.DS_Store`、`.dev-*.log`、`.dev.pid`、`.env` 均已由 `.gitignore` 覆盖且未跟踪 → **无需处理**（澄清，非删除项）。

---

## 4. 重复代码共享化清单

### 4.1 后端（高价值）

| # | 重复点 | 现状 | 共享方案 | 风险 |
|---|---|---|---|---|
| B1 | **事务提交散落** | `await self.db.commit()` 142 处（37 文件），风格不一、无统一回滚；仅 72 处方法末提交，**70 处中途提交**（部分流程依赖早期提交可见性，如 auth_service 提交后查 totp/发 token）、19 处条件提交（`if commit:`）、24 个方法多次提交、27 处 try/except 窗口 | ~~`app/core/` 引入 `@transactional` 装饰器或 `async with transaction(db)` 上下文管理器~~ → **有意延后（2026-08-19 决策）**：全量迁移会把 70 处中途提交改为方法末提交、改变事务边界语义，零功能收益、回归风险高；触发条件：**新代码强制 `@transactional`** / **单独排期逐方法迁移 + 配测试**（同门槛#5「有意延后」处置） | 中高：**已决策延后** |
| B2 | **Repository 分页** | `offset+limit` 手写于 8 个 repo（community/tools/rbac/notification/event/user/audit_log/exception_log） | ~~`BaseRepository.paginate(stmt, skip, limit)`~~ → **模块级 `paginate(stmt, skip, limit)` helper**（仅 `user_repo` 继承 `BaseRepository`，类方法对另外 7 个独立 repo 无效） | 低：纯查询层，**已执行**（BE `5c4ba8d`，17 处收敛） |
| B3 | **分页响应模型不一致** | 规范 `PaginatedResponse`（skip/limit） vs `EventListOut`/`AdminUserListOut`（page/page_size）；`admin_events.py:71` 与 `user_service.py:204` 手算 `total_pages` 且边界算法漂移（0 数据时 0 vs 1） | ~~统一复用规范模型（或新增 `page/page_size` 形态基类，全项目二选一）~~ → **保留两模型结构（不破坏契约）**：新增共享 `compute_total_pages()`（`max(1, ceil())` + 除零守卫），`PaginatedResponse` 内部与两处手算全部改用它；顺带收敛 `user_service.py` 列表查询为 `paginate()` | 中：**已执行**（2026-08-19；模型结构不变 → 无契约/前端改动） |
| B4 | **时区绕过集中工具** | `datetime.utcnow()` 4 处（`auxilio.py:88,119`、`auxilio_agent.py:151`、`contribution_service.py:79`） | 替换为 `app/core/timezone.py` 的 `now_utc()` | 低 |
| B5 | **alembic 双 head** | ~~主链 `4f5a6b...` 与分支 `2a3b4c...→d3e4f5...` 从未 merge~~ → **前提不成立**：21 个迁移为单链（`36bbae`→…→`d3e4f5`），唯一 head `d3e4f5a6b7c8`，`4f5a6b` 早已串入主链末尾（文件注释自证「串入主链末尾」） | ~~补 `alembic merge`~~ → **无需执行**（唯一 head 下 merge 为空操作） | 中：**已核实无需动作**（2026-08-19，离线解析 21/21 覆盖、线性无分支） |

### 4.2 前端

| # | 重复点 | 现状 | 共享方案 | 风险 |
|---|---|---|---|---|
| F1 | **日期格式化重复声明** | `src/app/tools/resource/use-resources.tsx:79` 本地重写 `formatDate`；全仓共 7 处自定义日期函数 | 统一 `@/shared/utils/utils` 的 `formatDate/formatDateTime/formatRelativeTime` | 低 |
| F2 | **Modal 背板/ESC 逻辑分散** | `admin/ui/*-modals.tsx`（event/user/role）、`submit-resource-modal.tsx` 各自实现遮罩与键盘监听 | 收敛复用 `components/primitives/modal-shell.tsx` / `confirm-dialog.tsx` | 中：视觉回归需比对 |

### 4.3 脚本层

| # | 重复点 | 现状 | 共享方案 | 风险 |
|---|---|---|---|---|
| S1 | **pg_dump 重复实现** | `scripts/db/export_db_to_desktop.sh` 重写 `pg_dump|gzip`，未调用 `CS-Web-Backend/tools/scripts/db/backup_db.sh` | ~~改为调用/复用 `backup_db.sh`~~ → **不可复用，保留自身实现**：`backup_db.sh` 加载 BE 仓 `.env` 且自带保留期清理（会把 `~/Desktop` 当备份目录误删旧备份） | 低中：**已执行**（仅修真 bug，见下注） |
| S2 | **健康探针重复** | `scripts/db/update.sh` 与 `scripts/db/healthcheck.sh` 各自内联 `docker compose exec` 探测 `/health` | 抽共享函数两脚本 source → **已执行**：`scripts/db/lib/health-probe.sh`（`probe_backend_health` / `probe_frontend_health`），`update.sh`（轮询）与 `healthcheck.sh`（单次）均 source 调用 | 低 |

> **执行附带修复（c146d38 重命名回归，2026-08-17）**：`update.sh` / `healthcheck.sh` / `export_db_to_desktop.sh` 从 `scripts/` 纯改名入 `scripts/db/` 时未同步 `$SCRIPT_DIR/..` 相对路径（少跳一级），三脚本原均指向不存在的 `scripts/.env` / `scripts/docker-compose.yml` 而整体失效；已统一修为 `$SCRIPT_DIR/../..`（根仓），并修正脚本头用法注释与 `RootDoc-Deploy.md` 残留的移动前路径。

---

## 5. 文档与配置治理清单

### 5.1 文档去重 / 合并

| # | 目标 | 问题 | 建议 | 可执行性 |
|---|---|---|---|---|
| D1 | `docs/api-reference.md` | 声称由 `openapi.baseline.json` 生成，实为手写，必然漂移（全仓无生成脚本） | (A) 接入真实生成器（`make gen-api-docs`）或 (B) 降级为查看器入口 + 显式"以 JSON 为准"标注 | **已执行**（2026-08-19 选 A：redocly → `docs/api-reference.html`，新增 `make gen-api-docs`；md 压缩为入口页 + README 索引改指 html） |
| D2 | `docs/项目待办事项-优先级重排.md` | 已闭环条目（P0-2/3/4、P1-1/3/4/6、P2-7）滞留正文，与 `CHANGELOG.md` 重复，违反治理"已收口项不留正文" | 删除已闭环条目，仅留进行中/延后/待评估项（预计减半） | **确定可做** |
| D3 | `docs/公共组件调研报告.md` §2.1 | ~~前端组件清单与 `RootDoc-FEArch.md` §2.1 一字不差重复~~ → **前提不成立**：FEArch §2.1 为方法论（Atomic Design 落地）、不含组件清单；报告 §2.1 是真实组件清单（比 `components/README.md`「分层」表更细） | §2.1 顶部加指针指向 `src/components/README.md`（权威清单）+ **保留详细清单**（防丢变体 / `use*` hook / `*Provider` 信息） | **已执行**（Phase 2 #9） |
| D4 | `CHANGELOG.md` | ① C-19 写成逐文件提交日志（21 行）；② `[0.9.9]` 出现两个 `### Changed` 和两个 `### Docs`；③ 无 `[Unreleased]` 占位却大量引用 | 压缩过程细节为结论摘要；合并重复小节；补 `## [Unreleased]` 占位；**不拆分历史**（防死链） | **确定可做** |
| D5 | `docs/RootDoc-Deploy.md` §八 | 仍称"死链审计仅 Jenkins 覆盖"，但根 `ci.yml` 已接入 | 更新 CI 分工描述 | **确定可做** |

### 5.2 配置 / CI 缺陷

| # | 目标 | 问题 | 建议 | 可执行性 |
|---|---|---|---|---|
| C1 | `Makefile:132` `check-contract` | `check-contract` = `contract-baseline` + `contract-check`，前者**静默覆盖基线**，后者永远通过 → 门禁失效 | 改为仅 `contract-check`；基线更新走显式 target | **确定可改**（对齐治理意图） |
| C2 | `.github/workflows/e2e.yml:38-47` | 与 `ci.yml:50-53` 重复跑 `check_version_sync.py`/`check_dead_links.py` | `e2e.yml` 仅保留 E2E，去重复检查步 | 需确认触发分支 |
| C3 | 子仓 `.github/workflows` | 与根仓 CI 职责边界需复核 | 见 `RootDoc-Deploy.md` 更新后的分工 | 待评估 |

---

## 6. 待确认项（二选一决策）✅ 两项均已于 2026-08-19 决策并落地

| 项 | 选项 A | 选项 B | 决策 |
|---|---|---|---|
| `admin-events-settings.tsx` | 删除（无挂载计划） | 接入 `admin-events-panel.tsx`（恢复功能） | **B 接入**：挂载到 `admin-events-panel.tsx`（工具栏新增 Settings 按钮展开内联设置面板） |
| `api-reference.md` | 接入真实生成器（redocly/widdershins） | 废弃 md，用 OpenAPI JSON + 在线查看器 | **A 接入生成器**：redocly → `docs/api-reference.html`（`make gen-api-docs`），md 压缩为入口页 |

---

## 7. 建议执行阶段（每阶段独立验证）

### Phase 1 — 确定可删/可改（低风险，先做）
1. 前端删除 3 个孤立文件（`app-error.ts`、`tenant-context.ts`、`admin-notifications-panel.tsx`）；
2. `use-resources.tsx` 的 `formatDate` 收敛到共享 utils；
3. 后端 `.gitignore` 追加 `uploads/`；9 文件去 BOM；`database.py` alembic.ini 路径常量化；
4. `Makefile` `check-contract` 修复；`CHANGELOG` 格式合并 + `[Unreleased]` 占位；
5. 文档 D2（待办已闭环条目清理）。

**验证**：`make check-version` + 前端 `vitest` + `ts-check` + 前后端本地启动冒烟。

### Phase 2 — 需谨慎确认（中风险）✅ 全部完成（2026-08-19；落地方式见 §3.2 / §4.1 / §4.3 / §5.1 纠错备注）
6. 后端分页收敛（B2，纯查询层）→ **模块级 `paginate()` helper**（方案原 `BaseRepository.paginate()` 前提不成立：8 个分页 repo 中仅 1 个继承 `BaseRepository`）；BE 已提交 `5c4ba8d`；
7. 前端依赖 `tw-animate-css`/`pino-pretty` 处理 → 仅 `pino-pretty` 移 `devDependencies`，`tw-animate-css` 保留；
8. 脚本层共享（S1/S2）→ 见 §4.3（含 c146d38 路径回归修复 + `lib/health-probe.sh`）；
9. 文档 D3/D5 更新 → D3 见 §5.1 行；D5 已按根仓 `ci.yml` 现状改述（ER-09 双覆盖）。

**验证**：后端 pytest（需 docker PG/Redis，子仓 CI 覆盖）+ 前端全量回归。

### Phase 3 — 高价值重构（配测试）
10. 事务 `@transactional` 统一（B1）⏸ **有意延后**（2026-08-19 决策，见 §4.1 B1 行；触发条件：新代码强制 `@transactional` / 单独排期逐方法迁移+配测试）；
11. 分页响应模型统一（B3）✅ **已执行**（2026-08-19：`compute_total_pages()` 统一算法 + 收敛 `user_service.py` 为 `paginate()`；模型结构保留，无契约变更）；
12. `utcnow()` → `now_utc()`（B4）✅ **已执行**（2026-08-19：4 处收敛 `now_utc()` + 3 模型列对齐 `DateTime(timezone=True)` + 修复 2 处潜伏 naive/aware TypeError）；
13. alembic merge（B5）✅ **已核实无需动作**（单链唯一 head `d3e4f5a6b7c8`，见 §4.1 B5 行）。

**验证**：全量 E2E（合并 main 触发）＋ 契约基线 `--check` 零漂移。

---

## 8. 落地要求

- 遵循协作约定：**一次只执行一个独立改动点，每步完成暂停等确认**；
- 所有删除动作先 `git rm`（保留历史），不做 `rm -rf`；
- 改动同步 SSOT：`docs/项目待办事项-优先级重排.md` 与根 `CHANGELOG.md`；
- 本方案文档登记于 `docs/README.md` 根级索引。

---

## 附：已排除项（澄清，勿重复审计）

- 后端 `worker.py` / `rbac_init.py` / `db_initializer.py`：经 arq / 生命周期 / CLI 引用，非死代码；
- 前端 26 个 `*.test.*` 文件：vitest 测试套件，非死代码；
- 根仓临时文件（`.DS_Store`/`.dev-*.log`/`.env`）：已被 gitignore 覆盖，无安全风险；
- `openapi.baseline.json`：仅根仓一份，无子模块重复基线；
- 环境变量三处罗列（Deploy §2 / EngConv §5.1 / Onboarding）：教程 vs 权威的有意分工；
- `data/`、`__pycache__`：已被正确忽略，无需处理。
