# 项目待办事项

> 未完成任务唯一权威跟踪(SSOT)。已收口项统一归档根 `CHANGELOG.md`,正文不留 ✅。
> 格式:`状态 编号｜描述:正文`;补充字段内联 `` `字段:值` ``(来源/建议/实施/结论/关联/收益/风险,`;` 分隔)。状态:🔄 进行中 ｜ ⚠️ 部分缓解。优先级:P0 发布前清零 ｜ P1 本波次收口 ｜ P2 可暂缓(规模/远期触发) ｜ P3 观察/可选。

> 📌 **重排快照(2026-08-18)**：基于代码实况 + CHANGELOG 重排的优先级见 [`项目待办事项-优先级重排.md`](./项目待办事项-优先级重排.md)。已核查 CS-03 已闭环、CS-02 框架已落地、AR-S2 路径过期但问题仍在(扩散至 4 处)、R17 约 85% 完成。请据该文"第四节"同步本 SSOT（最小化改动）。

---

# P0 急于落地(1.0.0 收口前须清零)

> 判定依据:1.0.0 发布门槛表未完成项(契约门禁✅、管理员 2FA✅、ER-01✅ 已闭环,其余见下)+ 数据安全项。

- **未标注日期-R17 审计严格落库**:`audit_service.record` 默认 best-effort,失败仅 warning;仅 `record_atomic(strict=True)` 严格落库。
  `建议:关键操作改 strict=True` `关联:ER-49` `门槛:1.0.0 #2 管理员操作审计不丢`
- **AR-S2 周期任务幂等**:`maintenance_tasks.py:31` 用 `asyncio.create_task`,跨实例可能重复执行。
  `建议:改 arq cron 单点调度` `关联:ER-37` `门槛:1.0.0 #4 多实例无重复执行`
- **D10 1.0.0 功能边界拍板**:DM/Wiki/活动评价/相册 须纳入 1.0 或显式延后(写 CHANGELOG)。
  `门槛:1.0.0 #6` `关联:P2 M1/M2/M4/M5 定位`

---

# P1 近期推进(本波次收口)

> 来源:工程保障全栈审查(ER-15~47)+ 文档治理/代码符合性自检 + 架构评审波次4(2026-08-17)。已收口组(代码质量 ER-15/22/24~28/30/32、运维 ER-29/33~40+Node 统一、测试 ER-41~47,432 passed)明细见根 `CHANGELOG.md` [0.9.9]。

## 波次4 架构重构(2026-08-17 登记,建议 0.9.9 发布后)

> 来源:架构评审(2026-08-17)M-A1/A2/A3、D-A3/A4。均按 ER-15 已验证范式(基线测试→抽纯函数→拆类→契约零漂移+CI 闸门)分阶段推进,每步可回滚。清理类 C-1~C-14 已落地,不重复登记。

- **C-17 🔴 ARC｜上帝服务拆分(User/Auth)**:`user_service.py` 844 行、`auth_service.py` 775 行单类。
  `建议:复用 ER-15 范式拆 User/UserAdmin/Profile/Password + Auth/Token/OAuth` `收益:单类≤350 行、可并行` `风险:最高,分阶段+集成测试对照`
- **C-16 🟡 ARC｜service 直查下沉 repo**:service 层 28 处 `self.db.execute`(`community_post._enrich_posts`、`user_service:500` 等)。
  `建议:下沉 repository,repo 为唯一查询入口` `关联:ER-51` `风险:中低,逐处迁移+回归`
- **C-19 🟡 FE｜前端数据获取收敛**:40 个模块文件手写 `fetch+useState`。
  `建议:封装 useApiRequest(SWR:loading/error/去重/mutate)` `关联:M10、X-A2` `风险:中,渐进`
- **C-15 🟡 FE｜BFF 样板生成**:141 个 `route.ts` + `backend-client.ts`(594 行)每路由重复参数解析/错误归一/字段映射。
  `建议:openapi.baseline.json 生成 route 骨架(路径+转发+错误归一),或先抽 mapTo 助手低风险起步` `收益:新增 API 成本↓70%、消除 1000+ 行重复`
- **AR-ACT1 🟡 FE/BE｜活动合并入工作台(方案 A)**:工作台作活动统一入口未落地。
  `实施:① ActivityService 门面(列表/详情/报名/签到/评价/回顾) ② MessageHandler 加 activity 路由 ③ state.py 加活动状态位 ④ tools.md 补「活动」 ⑤ README 工作台段加入口 ⑥ tests 全流程` `关联:P2 M4/M5`

## 架构评审·其余建议(2026-08-17,非重构面)

- **AR-M1 🟡 ARC｜资源访问门面 `ResourceService`**:README 经 `open()/glob()` 直读仓库文件,五方法散落各 module。
  `建议:集中 read_docs/get_doc/glob_docs/read_config/open_shared_resource,内部禁 ".." 向上逃逸;openapi.baseline.json 改运行时加载` `风险:中低,配 tests/test_resource_service.py(禁逃逸+白名单)`
- **AR-M2 🟡 ARC｜`config.py` 门面收敛**:`config_dir_safe` 与 `Path(__file__).resolve().parent.parent` 散落,config.yaml 加载入口不统一。
  `建议:删 config_dir_safe,新增 paths.get_static_root()/get_config_root(),统一加载入口` `风险:低,test_config.py 回归`
- **AR-D1 🟡 ARC｜拆分 `validate_and_register`**:单函数含 validate_inputs/validate_message/register_and_respond/persist_session 四职责。
  `建议:拆四函数;request 级中间件管理会话(替代函数内手动关闭)` `风险:中,补 tests/test_register_flow.py`
- **AR-P1 🟡 ARC｜个人日程隐藏外部总线**:`PersonalScheduleService` 直接暴露 `RedisStreamBus`。
  `建议:仅暴露 register_schedule/cancel_schedule/list_schedules,register 接受 schedule 重载(本地/远程)` `风险:中低,补 test_personal_schedule_service`
- **AR-X1 🟡 ARC｜验证器 / `ValidationResult` 收口**:各 validate_* 返回布尔且行为不一(validate_community 直接 raise HTTPException)。
  `建议:ValidationResult dataclass(is_valid/errors/value)+ validate_payload 门面统一` `风险:低,test_api_validators 回归`
- **AR-S1 🟡 SEC｜`/health/security` 暴露配置**:无鉴权端点返回 AUTH_ENABLED/redis 配置。
  `建议:脱敏或加简单鉴权` `关联:ER-48(合并跟踪)`

## 本轮新增建议(2026-08-13 全栈审查,去重后)

> 已与既有待办去重(CJK 字体/Sentry/requirements/多 worker 限流/FTS/表名拼接/日志脱敏等由既有条目覆盖),仅列新增项。

- **2026-08-13-CS-02 🟢 P1｜前端测试盲区**:前端 `src` 搜 `*.test.ts*` 0 命中,组件/hooks 无单测。
  `建议:Vitest+RTL,先 primitives/(Button/Input/ConfirmDialog)与 hooks(use-auth/use-feature-visibility),再扩动效组件` `关联:前端覆盖率门禁已由 ER-45 落地`
- **2026-08-13-CS-03 🟢 P1｜`server.ts` 手写 .env 解析**:`src/server.ts:18-33` 自写逐行 parse,对 `#`/引号/多行值边界处理粗糙;`user-menu.tsx:11` 有 `;;` 笔误。
  `建议:dotenv 替换(override:false);lint 加 no-extra-semi 修 user-menu.tsx`

---

# P2 可暂缓(规模/多实例触发 或 远期新功能)

## 发布门槛延后 / 近期关注(部分缓解但本质未解)

- **⚠️ 2026-08-01-P2/L4+L5 外部监控接入**:前端已可选 Sentry(monitoring.ts 动态 ensureSentry)+错误率监控;后端未接入(无 sentry_sdk),有 OTel 替代。
  `说明:1.0.0 门槛 #5,依赖外部服务暂缓;支撑 Ops-SLI/R18 探针` `关联:ER-06`
- **🔄 2026-07-31-Ops-ErrorBudget**:2026-08 月度 Error Budget 记录——`FrontDoc-Ops.md` Part B 历史表框架已建,待首个完整月数据。
  `说明:随月度 SLI 数据积累回填;联动 Ops-SLI/R18`
- **🔄 2026-07-31-Ops-SLI / R18**:可用性 SLI 外部探针未接入,现为应用层日志统计降级方案。
  `建议:接入外部探针(Sentry/GlitchTip 等)` `关联:P2/L4+L5`
- **⚠️ 2026-07-31-EX-1 灾备能力缺失**:已有每日 03:00 全量备份 sidecar(RPO 24h),仍单 PostgreSQL 主库、无 replica/故障转移。
  `结论:现状风险接受(<200 用户),目标为多区域容灾,待规模逼出需求再立项`

## 波次4 次级项(🟢 低风险/低优先)

- **C-18 ARC｜repository 巨型文件拆分**:`community_repo.py` 718 行(7 类)、`tools_repo.py` 621 行。
  `建议:按子域拆 4~5 文件(post/comment/interaction/follow/report+series),与已拆 service 对齐` `风险:低(纯搬移)`
- **AR-M3 小 M 重构**:`validate_project_session_exists` 改查 `user_purpose` 表(替代 project_sessions 旁路);`actions/community.py` 减 `time.sleep` 依赖;`deps.py` 改依赖注入。
- **N-01 SEC｜密钥轮换登记(已确认结果)**:`RootDoc-SecFor.md` 补 ER-11「手动密钥轮换(临时)」登记;`.env.example/.env.production/.env.staging` 已核对无真实密钥。
  `建议:补轮换流程登记,与 ER 安全体系合并`
- **N-12 / N-13 / F-9 低风险增强**:① 前端 `AGENTS.md` 补「外链/凭证/绝对路径」红线段 ② `tag_and_release.sh` 补 changelog 自动撰写 ③ 本地 `alembic heads` 实测复核,修正文档 heads 描述(文档冲突审查 F-9 标注「未做」)。

## 远期新功能(单实例 <200 用户下不紧急)

- **2026-08-01-2.0 微服务拆分(Part C)**:全仓 0 个 `.java`;Auxilio 仍单体内 service,阶段 0–4 均未启动。
  `阶段:0 单体巩固(含 M10 前端 Repository 抽象层)→ 1 Python 试点(Auxilio 迁 FastAPI)→ 2 Java 试点(签到迁 Spring Boot)→ 3 事件总线跨语言 → 4 全面拆分` `⚠️ ADR-ROADMAP-001 建议从 2.0.0 推迟至 3.0.0+,2.0.0 仅做单体巩固+插件化(待拍板)`
- **2026-08-01-P1/M1 私信 DM**:关注/动态流 ✅ 已实现;私信 ❌,全库无 dm/conversation 模块。
  `关联:1.x「社区四件套」;D10(纳入或延后待拍板)`
- **2026-08-01-P1/M2 Wiki/知识库、M4 活动评价与反馈、M5 相册/活动回顾**:均未实现(搜 `wiki|知识库|协同`、`gallery|album|瀑布流`、rating/review 类型 0 命中)。
  `关联:1.x「社区四件套」;D10`
- **2026-08-01-P2/L1 内网聊天、L2 RSS/邮件订阅、L7 数据归档冷存储、L8 未读通知 SSE 推送、L10 GraphQL 网关**:均未实现(0 命中;notification-bell 仍轮询)。
  `关联:L1/L8→1.x 实时化(先 SSE 后 WebSocket);L10→2.0 API-first`
- **2026-08-05-文档迁移×3**:后端 docs 只读托管/API、提炼跨仓库通用规范进 Devdocs-*、DevCenter 加后端文档源 Tab——均未做,依赖 docs 托管服务搭建。
- **2026-08-01-Devdocs-Arch-3.3 架构优化**:M10 前端 Repository 抽象层(现直接用 `backend-client.ts`,属 Part C/阶段0 前置);L9 定时任务前端 UI 缺失(后端已实现)。

## Roadmap 新增功能缺口(N1~N4,2026-08-11 D9 已拍板补列)

- **2026-08-09-Roadmap-N1 全局统一搜索**:仅社区有 GIN 全文检索,活动/工具/用户跨域检索缺失。`建议:1.x 建跨域搜索服务`
- **2026-08-09-Roadmap-N2 学校 SSO(CAS/LDAP)**:当前仅 GitHub OAuth,无法验证「真实在校成员」,高校社团硬需求。`建议:1.x 接入`
- **2026-08-09-Roadmap-N3 隐私数据导出/删除(类 GDPR)**、**N4 内容审核工具增强**:UGC 审核/举报处理后台。`建议:1.x 合规项`

## 工程审查低危项(ER-48~ER-57,🟢)

- **ER-48 SEC｜/health/security 无鉴权探测**:`app/main.py:211-257` 返回 AUTH_ENABLED/redis 配置(与 AR-S1 合并跟踪);**ER-49 ERR｜通知副作用失败仅 debug**:关键通知升 error/warning(关联 R17)。
- **ER-50 PRF｜FTS 配置名拼接**、**ER-51 COD｜表名 f-string 拼接(含 CodeGov-B3)**:改白名单映射查表(均无注入风险,属写法收敛)。
- **ER-52 DEP｜nodemailer 放 dependencies**:`CS-Web-Frontend/package.json:32`,移 server-only 服务端专用依赖。
- **ER-53 ARC｜前端 scrypt 边界**:已由 check-bff-boundary CI 兜底(AL-1 落地),闭环。
- **ER-54 CFG｜非密钥默认值写死**、**ER-57 OPS｜缺全局日志脱敏**:纵深防御项。

---

# P3 观察 / 可选(非致命、有意保留或可选增强)

- **⚠️ 2026-08-09-CodeGov-F1 残余｜CJK 字体本地化(1.x 优化)**:拉丁 3 字族已本地化(~123KB,外链 5→2);CJK 结论保持现状(自托管子集化成本高、国内 CDN 不稳、已有系统回退链)。
- **2026-08-13-CS-07 `export_db_to_desktop.sh` 敏感数据标注**、**CS-08 过程性文档归档**(2026-08-17 已合并演变历史分卷,范围缩小)。
- **2026-08-05-前端设计-EmptyState**:共享空状态组件已建,其余手写空状态可渐进迁移(原 ~28 处已减少)。
- **2026-08-05-前端评审-不做1/2/3**:不补 ui/ 域、不补 index.ts 桶、不收敛徽章/模态壳(决策记录,有意保留)。
- **2026-08-05-i18n-环境1 dev preload 报错**:Next16+ 自定义 server 已知非致命错误,`preload` 实为 HSTS 字符串非函数调用。
- **2026-08-09-DocGov-D FrontDoc 命名约束例外**:命名约束处明示 `capsule-tabs.md` 例外,防后续误判。
- **2026-08-09-CodeGov-B2 命名后缀漂移**:`exception_log_stats.py` 缺 `_repo`、services 下 6 文件缺 `_service`:补后缀或 BackDoc-Conv §2 明示例外。
- **ER-58 TST｜E2E 跑 dev 模式**:`playwright.config.ts:22` 跑 dev 含 HMR,CI 改 preview/生产构建。
- **ER-59 DOC｜历史归档表模板残留**:后端 `tools/docs/README.md:86-92` 空表头清理。
- **2026-08-09-R3 caddy 残留**:`scripts/healthcheck.sh` 引用 compose 不存在的 caddy 服务;**R4 根 .env.example 缺 LLM_***:补 optional 示例(与 RootDoc-Deploy §八对齐)。

---

> 各文档「提交前自查 Checklist」模板属 PR 自检规范,非项目级待办。
> 已闭环任务归档于根 `CHANGELOG.md`(变更记录主索引);本文件仅保留经核查仍真实存在的未完成任务。
