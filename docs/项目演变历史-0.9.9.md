# 项目演变历史 · 0.9.9

> ⚠️ **归档只读**：本文件属于版本演变历史，**仅供追溯，不得修改**。
> 活文档（L0/L1/L2）**不得链接本文件、不得从中提取活内容**；活内容归属见 [`DocGovernance.md`](./DocGovernance.md) 所有权矩阵。

> 版本范围：**0.9.9**（0.9.8 之后的迭代波次）。
> 覆盖日期：2026-08-07 ~ 2026-08-09。
> 编号体系见 [`项目演变历史.md`](./项目演变历史.md)（索引）。
> 前序版本：← 0.9.8（见同名拆分文件）。

---

## 一、工具页 → 工作台改造方案（原 `tools-workbench-redesign-plan.md`，规划 2026-08-08，部分落地 0.9.8）


> ⚠️ **历史方案**：工具页 → 工作台改造方案（2026-08-08 规划）。**已部分落地（0.9.8）**：工作台模块（`src/modules/workbench`，registry 配置驱动、视图切换、数据备份）与各 widget（greeting / today-tasks / pomodoro-player / exam-countdown / quick-notes / github-heatmap / llm-usage / assistant-chat）已实现，后端 workbench / auxilio 路由与数据表已上线。本文件保留为**方案对照记录**，其中规划细节可能滞后于实际实现，与代码冲突处以代码与 `openapi.baseline.json` 为准。

> 基于代码库实地调研（2026-08-08）。前端：Next.js 16 App Router + Tailwind 4 + SWR + next-intl，无图表库；后端：FastAPI + SQLAlchemy 2.0 async，JWT + GitHub OAuth，asyncio 后台任务。

### 0. 现状盘点（与方案强相关的事实）

| 现状 | 对方案的影响 |
|---|---|
| `src/app/tools/page.tsx` 工具卡片硬编码在 `TOOLS` 数组 | 改造需引入「模块注册表」机制，卡片配置化 |
| 已有 GitHub OAuth（`oauth_service.py`），User 模型含 `github_id` | GitHub 热力图可直接复用 OAuth token，用户零成本授权 |
| LeetCode 无任何集成代码 | 需走非官方 GraphQL 端点，有风险需降级设计 |
| Auxilio 学习助手为纯规则推荐，无 LLM 调用、无统计表 | API 调用统计需新建埋点表 + 聚合接口 |
| 无 echarts/recharts/d3 | 热力图用 CSS Grid、趋势图用手写 SVG，避免引入重依赖 |
| 有 asyncio 后台循环（token_gc/data_retention） | 复用该模式做热力图数据的定时抓取与缓存 |
| 页面文案走 next-intl | 所有新模块文案需补 zh/en 词条 |

---

### 1. 核心模块评估

#### 模块一：第三方平台贡献热力图（GitHub + LeetCode）

**价值：★★★★★　可行性：GitHub ★★★★★ / LeetCode ★★★☆☆**

- **数据获取**
  - *GitHub*：复用现有 GitHub OAuth 的 access token，后端调用官方 GraphQL `contributionsCollection.contributionCalendar`（一次请求拿全年 365 天数据）。需在 OAuth scope 中确认包含 `read:user`。
  - *LeetCode*：无官方公开 API。可行路径是 `leetcode.cn/graphql` 的 `userCalendar` 查询（公开资料仅需用户名，无需登录），或让用户手动填 Cookie 提升稳定性。**这是全方案最脆弱的一环**，必须做熔断降级：接口失败时展示最近一次缓存 + 「数据更新于 X 前」提示。
- **展示形式**：两张 53×7 的方格热力图（CSS Grid 手写，约 80 行代码，无需图表库），配色复用现有 CSS 变量体系（绿阶），hover tooltip 显示日期+次数，右侧附「近一年总提交/刷题数、当前连续天数、最长连续」三个数字徽章。LeetCode 卡片额外提供「绑定用户名」入口。
- **更新频率**：热力图是低频数据，**后端每 6 小时抓取一次**（复用 asyncio 后台循环），结果落库缓存（新表 `contribution_cache`）。前端只读缓存，SWR `refreshInterval: 5min` 或干脆不轮询。绝不让前端直连第三方——避免 CORS、token 泄露和限流。
- **技术难点**：GitHub token 过期/用户撤销授权 → 抓取失败时保留旧数据并在卡片角落标注「授权失效，请重新连接 GitHub」；LeetCode 端点变动 → 抓取逻辑隔离成独立 service，失败自动熔断 + 指数退避。

#### 模块二：时间信息（当前时间 + 会话在线时长）

**价值：★★★☆☆　可行性：★★★★★（半天工作量）**

- 纯前端模块，零后端依赖。当前时间用 `setInterval(1s)`；会话开始时间写 `sessionStorage`（新开标签页重置、刷新保留），在线时长 = now - start。
- 建议扩展为「问候条」：按时段显示「早上好/下午好/晚上好 + 用户名」，把时间、日期、星期、在线时长整合成页面顶部的欢迎区，顺带承担工作台的个人化氛围。
- 难点几乎没有，注意组件卸载时 clearInterval，以及 next-intl 的日期本地化格式。

#### 模块三：番茄钟

**价值：★★★★☆　可行性：★★★★★（第一期纯前端即可上线）**

- 第一期：纯前端实现，配置（工作 25min / 短休 5min / 长休 15min / 长休间隔 4 轮）存 localStorage；计时状态也持久化，**刷新页面不丢进度**；状态提示用标题栏闪烁 `document.title = "🍅 休息中"` + 页面内状态色环，不做浏览器 Notification（权限弹窗体验差，可选增强）。
- 第二期：专注记录落后端（新表 `focus_sessions`：开始/结束/时长/是否完成/关联任务），为「周专注时长统计」供数——这让番茄钟从玩具变成数据资产。
- 难点：浏览器 tab 休眠时 `setInterval` 不准 → 用「目标时间戳 - Date.now()」而非倒数计数，从根上免疫休眠漂移；声音提示用 WebAudio 合成，不引外部音频文件。

#### 模块四：学习助手 API 调用统计

**价值：★★★★☆　可行性：★★★★☆（需后端新建埋点）**

- **埋点**：新建 `api_call_logs` 表（user_id、endpoint、method、status、latency_ms、created_at），用 FastAPI 中间件统一记录，不只覆盖 Auxilio——全站 API 都有了可观测性，一石二鸟。
- **聚合**：新建 `/api/v1/stats/api-usage` 接口，返回今日调用数、近 7/30 天按日聚合、按 endpoint 分布。查询频率低但数据量会涨，**用每日预聚合 rollup 表**（复用后台循环每天凌晨跑），避免每次现算大表。
- **展示**：今日调用计数器（大数字卡片）+ 近 30 天调用趋势（手写 SVG 折线/柱状，单系列数据手写成本远低于引入 echarts）+ Top 5 接口占比（水平条形，纯 CSS 宽度即可）。
- **难点**：高并发下中间件写库开销 → 异步 fire-and-forget 写入，失败不影响主请求；rollup 任务失败要有重试。

---

### 2. 建议新增模块（额外 6 个）

| 模块 | 说明 | 价值 | 可行性 |
|---|---|---|---|
| **今日任务聚合** | 复用现有 task 工具数据，置顶展示「今天到期 + 已逾期」任务，逾期标红，一键完成。**这是「工作台」区别于「工具页」的灵魂模块**——打开页面就知道今天该干嘛 | ★★★★★ | ★★★★★ |
| **考试倒计时** | 复用 exam 工具的考试日期，卡片显示「距 XX 考试还有 N 天」，多个考试轮播或列出最近 3 个 | ★★★★☆ | ★★★★★ |
| **薄弱知识点摘要** | 复用 Auxilio 现有的错题→薄弱标签逻辑，展示 Top 3 薄弱点 + 「去复习」直达链接，让学习助手在首屏有存在感 | ★★★★☆ | ★★★★☆ |
| **快捷便签** | 临时想法速记，localStorage 存储 + JSON 导出，零后端成本；后期可升级为多端同步 | ★★★☆☆ | ★★★★★ |
| **系统状态角标** | 面向开发者/管理员：后端健康、数据库连通、版本号，复用 dev-center 能力，仅管理员角色可见 | ★★★☆☆ | ★★★★☆ |
| **每周复盘卡** | 每周一展示上周数据汇总：完成任务数、专注时长、提交/刷题数、API 调用趋势，一卡览全周（依赖番茄钟二期和统计模块的数据积累） | ★★★★☆ | ★★★☆☆ |

> 明确**不建议**做的：天气/一言等纯装饰第三方 API（增加外部依赖却无生产力价值）、站内聊天/社交（偏离定位）、音乐播放器（与番茄钟的声音提示职责重叠且复杂）。

---

### 3. 布局与信息层级设计

**设计原则**：从「工具陈列架」变成「信息仪表盘」——用户打开 3 秒内回答三个问题：*今天要干什么？我最近在坚持吗？现在该专注还是休息？*

#### PC 端（≥1024px）：12 列网格

```
┌──────────────────────────────────────────────────────┐
│  问候条：早上好，{name} · 08-08 周六 · 在线 32min      │  ← 全宽，模块二
├──────────────────────────────┬───────────────────────┤
│  ⚠ 今日任务（逾期标红置顶）    │  🍅 番茄钟              │  ← 左 8 列 / 右 4 列
│  （模块：今日聚合）            │  （模块三）             │
├──────────────────────────────┤  ⏳ 考试倒计时          │
│  GitHub 热力图（近一年）       │  📝 快捷便签            │
├──────────────────────────────┤  ─────────────         │
│  LeetCode 热力图（近一年）     │  📊 今日 API 调用：N    │
├──────────────────────────────┤     （计数器卡片）       │
│  API 调用趋势图（30 天）       │  🎯 薄弱知识点 Top3     │
├──────────────────────────────┴───────────────────────┤
│  原工具入口收编为底部「全部工具」紧凑网格                │
└──────────────────────────────────────────────────────┘
```

- **层级逻辑**：第一屏 = 行动（任务 + 番茄钟）；第二屏 = 坚持（热力图）；第三屏 = 复盘（统计趋势）。原有工具卡片入口不删除，收编到底部保持可发现性，老用户无迁移成本。
- **移动端（<768px）**：单列堆叠，顺序即优先级：问候条 → 今日任务 → 番茄钟 → 倒计时 → 热力图（横向可滚动）→ 统计 → 便签。
- **工程实现**：建一个 `widget-registry.ts`（模块 id → 组件 + 默认位置 + 所需数据 hook），页面按注册表渲染；后期加「自定义排序/显隐」时只需把顺序配置存用户偏好，骨架不用动。

---

### 4. 潜在技术难点与应对总览

| 难点 | 风险 | 应对 |
|---|---|---|
| LeetCode 无官方 API | 端点随时可能变动/加验证 | 独立 service 隔离 + 熔断 + 缓存兜底 + 失败时明示「数据非实时」，不让整页报错 |
| GitHub token 安全与失效 | token 泄露或用户撤销授权 | token 只在后端使用、不落前端；抓取失败保留旧数据 + 重新授权引导 |
| 热力图性能 | 2 张 × 365 格渲染 | 纯 CSS Grid 无压力；数据后端缓存，前端不做聚合计算 |
| API 统计写库开销 | 高频请求拖慢主流程 | 中间件异步写入 + 每日 rollup 预聚合，查询只打小表 |
| 实时性的伪需求 | 有人会觉得「越实时越好」 | 明确各模块刷新策略：时钟 1s（本地）、番茄钟本地、热力图 6h、统计 5min 轮询——**不做 WebSocket**，当前数据密度用 SWR 轮询完全够 |
| i18n 与角色可见性 | 新模块文案、管理员模块泄漏 | 词条随模块同步补 zh/en；系统状态模块前端按 RBAC 角色条件渲染、后端接口同样鉴权 |

---

### 5. 分阶段改造路线

#### Phase 1：骨架 + 纯前端快赢（约 1 周）
- 搭建 widget-registry 骨架与新布局（问候条、左右分栏、移动端单列）
- 模块：时间信息、番茄钟（localStorage 版）、今日任务聚合、考试倒计时、快捷便签
- 原有工具入口收编到底部网格
- **全部无后端依赖，风险最低，上线即见形态**

#### Phase 2：数据化（约 1~1.5 周）
- GitHub 热力图：后端 service + GraphQL 抓取 + `contribution_cache` 缓存表 + 6h 定时刷新
- API 调用统计：`api_call_logs` 埋点中间件 + rollup 预聚合 + 统计接口 + 前端手写 SVG 趋势图
- 薄弱知识点摘要（复用 Auxilio）

#### Phase 3：补全 + 个性化（约 1 周）
- LeetCode 热力图（含绑定用户名流程、熔断降级）
- 番茄钟二期：专注记录落库、周专注统计
- 工作台自定义：模块排序/显隐，偏好存用户表

#### Phase 4：纵深（按需）
- 每周复盘卡、系统状态角标（管理员）、浏览器 Notification 可选增强、数据导出

#### 优先级排序（按「价值 ÷ 成本」）
1. 今日任务聚合（价值最高、成本最低，定义工作台灵魂）
2. 时间问候条 + 番茄钟（纯前端快赢）
3. GitHub 热力图（OAuth 现成，视觉冲击力最强）
4. API 调用统计（一石二鸟的可观测性）
5. 考试倒计时 / 便签 / 薄弱点摘要（小而美）
6. LeetCode 热力图（价值高但外部依赖最脆弱，放后面）
7. 每周复盘 / 自定义布局（依赖前期数据积累）

---

### 6. 扩展方向 A：学习助手 LLM 化（类网页 LLM 工作台）

#### 6.1 目标形态

首页升级为「学习助手｜工作台」：主区域是类 ChatGPT 的对话界面，副区域承载信息模块。学习助手从「规则引擎 MVP」升级为「LLM Agent」——既能闲聊、答疑，也能调用项目内现有能力（薄弱点分析、任务、资源库、考试倒计时、统计数据）。

#### 6.2 后端架构（新增）

```
前端 /tools/auxilio（对话 UI）
   │  fetch POST /api/v1/auxilio/chat（SSE 流式）
   ▼
FastAPI 路由
   │ ① 鉴权（JWT）② 查询会话上下文（conversations/messages 表）
   ▼
AuxilioAgentService（新建）
   │ ③ 组装 system prompt（注入 analyze_learning_profile 的学习画像 + 用户信息）
   │ ④ 调 LLM（OpenAI 兼容协议，httpx 流式，供应商可配置）
   │    └── 收到 tool_calls → 查工具注册表 → 执行 → 结果回传 → 继续生成
   │ ⑤ 落库消息 + 写 api_call_logs 埋点（token/延迟/状态，喂给统计模块）
   ▼
StreamingResponse（SSE：data: {delta} / {tool_call} / {done}）
```

**关键技术决策**

| 决策点 | 选择 | 理由 |
|---|---|---|
| LLM 协议 | OpenAI 兼容 `/chat/completions` + `stream: true` | 一套实现通吃 DeepSeek/通义/Kimi/OpenAI/本地 vLLM，`httpx` 已有依赖，无需新增 SDK |
| 配置 | Settings 新增 `LLM_BASE_URL` / `LLM_API_KEY` / `LLM_MODEL` / `LLM_TIMEOUT` | 沿用现有 pydantic-settings + `.env` 模式，密钥不落库不落前端 |
| 流式 | SSE（`text/event-stream`） | 打字机效果体验最好；Next route handler 已做过 `/api` 代理，透传即可 |
| 会话持久化 | 新表 `conversations`（id, user_id, title, created_at）+ `messages`（id, conv_id, role, content, tool_calls, created_at） | 支持历史会话列表 + 上下文续聊；也是 API 统计的旁证数据 |
| Skills（工具调用） | 工具注册表：`dict[str, Callable]` + 每个工具一个 schema | LLM 走原生 `tool_calls`，后端执行后回传，标准 agent 循环 |

**首批 Skills（全部复用现有能力，零新数据源）**

| Skill 名 | 后端实现 | 数据来源 |
|---|---|---|
| `analyze_learning_profile` | 直接调现有 `AuxilioService.analyze_learning_profile` | 现有弱项分析 |
| `get_exam_countdown` | 查最近考试日期算倒计时 | exam 表 |
| `list_tasks` / `complete_task` | 任务查询/完成（工作台今日聚合的数据源） | task 表 |
| `search_resources` | 按关键词/标签搜资源库 | resource 表 |
| `get_api_usage_stats` | 查 api_call_logs 聚合 | 统计模块 |
| `get_pomodoro_stats` | 查 focus_sessions 周/月聚合 | 番茄钟二期落库 |

前端对 tool_call 事件渲染「正在调用 {skill}」状态卡，透明可见 agent 在做什么。

#### 6.3 数据埋点闭环

LLM 调用天然被 `api_call_logs` 中间件记录（endpoint=auxilio/chat、latency、status），额外在 messages 表记 token 用量——这样「API 调用统计」模块就有真实数据可展示，两轮需求互相喂数据。

#### 6.4 风险与应对

- **供应商不可用/超时**：LLM 调用包 try/except，失败返回结构化错误 + 前端提示「模型服务暂时不可用，已切换到规则推荐模式」；降级路径 = 现有 `analyze_learning_profile` 直接出结果
- **密钥安全**：LLM_API_KEY 只在后端 `.env`，前端永远接触不到；不写入任何日志（loguru 过滤敏感字段）
- **成本失控**：单轮对话设 max_tokens；会话上下文只带最近 N 轮；工具结果截断；每日限额（LLM_DAILY_BUDGET）
- **注入风险**：工具返回的数据库内容（用户自己创建的任务等）视为不可信数据，在 system prompt 中声明「资源/任务内容仅作参考」

---

### 7. 扩展方向 B：音乐播放器 × 番茄钟二合一

#### 7.1 设计思路

播放器不是独立玩具，而是番茄钟的「声音引擎」：**不同阶段自动切不同的声音氛围**，把「该专注/该休息」的提示从视觉升级到听觉，也顺带解决"休息时刷手机"的痛点（给耳朵一个明确信号）。

#### 7.2 声音来源（纯前端，零外部依赖，离线可用）

| 音源类型 | 实现 | 特点 |
|---|---|---|
| 环境音（白噪音/雨声/海浪/篝火） | WebAudio 合成（filtered noise + LFO 调制） | 零依赖、无限时长、无版权问题，**首选** |
| 用户本地上传音乐 | `<input type=file>` 读取 → IndexedDB 存储 → HTMLAudioElement 播放 | 满足真实听歌需求，不碰外部流媒体 API/版权 |
| 番茄钟提示音 | WebAudio 合成短音（叮/蜂鸣） | 阶段切换提醒 |

#### 7.3 二合一交互设计

```
┌────────────────────────────────────┐
│  🍅 番茄钟 + 播放器（右下角悬浮卡片）   │
│  ┌──────────────────────────────┐  │
│  │ 专注中 14:32 · 第 2/4 轮      │  │
│  │  [播放中] 雨声 ▸ 下一阶段：短休  │  │
│  └──────────────────────────────┘  │
│  阶段音联动：                       │
│  专注 → 自动播放「专注音」           │
│  短休 → 自动切「轻快音/音乐」        │
│  长休 → 停止或切「放松音」           │
└────────────────────────────────────┘
```

- **阶段-声音映射可配置**（localStorage）：每种阶段可选音源（环境音列表 / 上传音乐 / 静音）
- 用户手动切歌/暂停不打断番茄钟，但阶段切换时按映射恢复
- 页面底部常驻迷你播放条（进度 + 播放/暂停 + 切下一阶段音），移动端同样 44px 可点
- 番茄钟阶段切换时 `document.title` 闪烁 + 合成提示音双重提醒

#### 7.4 技术要点

- 倒计时依旧用「目标时间戳差值」而非 interval 累减（tab 休眠免疫），声音调度挂在时间戳到期回调上
- WebAudio 在无用户手势时无法启动 → 首次点击「开始专注」时创建 AudioContext 并 resume，顺带解决浏览器自动播放策略
- 上传音乐存 IndexedDB（localStorage 5MB 不够放音频），播放器用 IndexedDB + URL.createObjectURL 读取
- 播放器组件与番茄钟组件解耦（各自独立 hook），通过一个轻量 event bus（或组件内共享 state）做阶段联动，方便后期拆分复用

---

### 8. 修订后的总体阶段路线

| 阶段 | 内容 | 依赖 |
|---|---|---|
| P1（纯前端快赢） | 骨架 + 时间问候条 + **番茄钟×播放器二合一** + 今日任务聚合 + 倒计时 + 便签 | 无 |
| P2（数据化） | GitHub 热力图 + API 统计埋点/图表 + 薄弱点摘要 | 无 |
| **P3（学习助手 LLM 化）** | 对话 UI + SSE 流式 + 会话表 + **Skills 工具调用** + 降级兜底 | P1 的任务/番茄钟数据（成为 Skill 数据源） |
| P4（补全） | LeetCode 热力图 + 专注记录落库 + 布局自定义 + 复盘卡 | P2/P3 数据积累 |

> 说明：P3 的 LLM 接入需要供应商 API key（OpenAI 兼容协议即可，推荐 DeepSeek/通义，成本低）；Skills 复用 P1/P2 建好的数据表，所以顺序是「先有数据，后有 Agent」——Agent 不能凭空调用不存在的表。

---

### 9. 实施记录（2026-08-08 已落地）

#### 已完成

**P1 工作台骨架 + 纯前端模块**
- `src/modules/workbench/`：widget 注册表 + workbench 组装 + 数据备份（导出/导入/清空）
- 问候条（时间/日期/在线时长）、今日任务（个人待办，逾期标红置顶，示例数据）、快捷便签、考试倒计时
- 番茄钟×播放器二合一：WebAudio 合成环境音（雨/海浪/篝火/白噪音）+ IndexedDB 上传音乐 + 阶段自动切音 + 标题栏闪烁提示 + 刷新不丢进度
- `/tools` 页改造：Hero 改「工作台」，底部收编原工具入口

**P2 数据化**
- 后端：`contribution_cache`/`api_call_logs` 表 + GitHub 公开抓取 service（6h 缓存、stale 降级）+ 埋点中间件 + 统计接口；alembic 迁移 `b0b1c2d3e4f5`
- 前端：CSS Grid 热力图组件（用户名绑定/手动刷新）+ SVG 趋势图 + endpoint 分布

**P3 学习助手 LLM 化**
- 后端：`conversations`/`chat_messages` 表 + `llm_client`（OpenAI 兼容 + Anthropic 双协议流式 + 消息格式互转）+ `auxilio_agent`（7 个 Skills 工具注册表 + 最多 3 轮工具循环 + 无 key 时自动降级规则模式）+ SSE chat 路由
- 前端：对话 UI（react-markdown + 流式打字机 + 工具调用状态卡 + 历史会话列表）+「工作台 / 学习助手」视图切换

#### 启用 LLM（在 CS-Web-Backend/.env 中配置）

```env
LLM_PROVIDER=openai            # openai | anthropic | none
LLM_API_KEY=sk-xxx
LLM_BASE_URL=https://api.deepseek.com/v1   # OpenAI 兼容网关（DeepSeek/通义/Kimi/本地）
LLM_MODEL=deepseek-chat
```

未配置时学习助手自动降级为规则推荐模式（可用但不可对话）。

#### 待办
- LeetCode 热力图（数据源脆弱，需熔断降级）

#### API 调用统计 → LLM 用量 + 用户自行接入 API Key（2026-08-08）
- 工作台「API 调用统计」改为**学习助手 LLM 用量**：`llm_usage_logs` 表（provider/model/prompt/completion/total tokens/latency）+ `llm_configs` 表（用户级配置，API Key AES-256-GCM 加密存储，复用 TOTP 加密原语）
- LLM 客户端：`stream_chat` 支持用户配置 overrides（> 全局 .env）；OpenAI 流式 `stream_options.include_usage` 解析 usage 尾块；Anthropic 从 message_start/message_delta 取 input/output tokens；usage 事件携带 provider/model
- Agent `run_chat` 自动读取用户 `llm_configs` 解密后传入；chat 路由收集 usage 事件写 `llm_usage_logs`
- 新接口：`GET /workbench/stats/llm-usage`（今日调用/tokens/30 天趋势/模型分布）、`GET/PUT /workbench/llm-config`（脱敏回显掩码 / 加密保存）
- 新 Skill：`get_llm_usage_stats`（总调用/今日/总 tokens）；迁移 `d3e4f5a6b7c8`
- 前端：`llm-usage-stats.tsx` 替换 api-usage-stats（统计卡 + tokens 折线 + 模型分布 + 「LLM 设置」面板：provider/API Key/Base URL/Model）；未配置时学习助手提示「在 LLM 设置中接入 API Key」
- mock 验证：usage 尾块解析（delta→usage→done）+ 用户配置覆盖生效；ts-check/eslint 全绿；路由 401 鉴权正常

#### 规范自检整改（2026-08-08，对照 docs/RootDoc-FEArch.md §6）
- **§6.3.2 硬编码颜色**：清除全部散落 hex，改项目令牌（`var(--destructive/--chart-1/--muted-foreground)`）与 Tailwind 语义色板（emerald/amber/red/orange/blue/green）；SVG stroke 无法用类名，色值集中收在 `widgets/pomodoro/constants.ts` 并注释色板来源（唯一豁免，已说明理由）
- **§6.2.1 组件复用**：输入框/日期/下拉改复用 `@/components/primitives/Input`（INPUT_CLASS），主按钮/次级/危险操作改复用 `Button`（btn-primary/outline/danger 等宽大写风格）；视图切换改复用 `InlineTabs`；textarea 因项目 Input 变体不支持 rows，改用 INPUT_CLASS 常量直写（项目组件能力边界，已说明）
- **§2.4 复用阈值**：番茄钟 508 行 → 拆分 `widgets/pomodoro/` 目录即模块（use-pomodoro 状态机 hook + settings/music 子面板 + constants + 桶导出），组合层 <150 行
- **§3.3/§3.7**：补 `workbench/index.ts` 桶导出 + `README.md` 组件清单与数据存储说明
- **§2.3/§2.6**：布局改 widget-registry 纯配置驱动（slot 分组渲染），新增「布局设置」面板（widget 显隐开关，偏好存 `wb_widget_prefs`）
- **React Compiler lint 适配**：hook 返回值混入 ref 会触发 `react-hooks/refs` 误报 → ref 改为组件创建、hook 接收参数传入；参数 ref 必须列入 useCallback 依赖（`preserve-manual-memoization`）
- 验证：ts-check + eslint（workbench 全绿）+ /tools 页面 200 渲染正常

#### 番茄钟专注记录落库（2026-08-08，闭环完成）
- 新表 `focus_sessions`（user_id/duration_seconds/phase/sound_source/started_at）；迁移 `c2d3e4f5a6b7`（head 前移）
- 后端路由：`POST /workbench/focus-sessions`（上报完成轮）+ `GET /workbench/stats/pomodoro`（总轮数/总分钟/今日/30 天分布）
- 前端 `use-pomodoro`：round 递增（完成一轮专注）时 fire-and-forget 上报（未登录/失败静默，不影响本地体验）
- 学习助手 Skill `get_pomodoro_stats` 改为查询真实 focus_sessions 数据（总轮数 + 今日专注分钟）

#### i18n 词条完善（2026-08-08）
- 热力图/统计/对话组件的硬编码文案全部移入 `workbench` namespace（24 个新键：heatmapTitle/Bind/Contributions/Streak/Less/More/Max/Stale、apiUsageTitle/Today/Errors/Latency/NoData、loginRequired/newChat/noConversations/loading/chatPlaceholder/chatIntro/requestFailed/networkError 等），interface + zhCN + en 三处同步
- 修复批量替换时的字符串内表达式误插（appendDelta 拼接）与词条重复/错值问题
- 验证：ts-check + eslint 全绿，页面 200


---

## 二、功能模块可见性管理方案（原 `FeatureModuleVisibility-Plan.md`，规划 2026-08-07，落地 0.9.9）


> 版本：草案 v1（2026-08-07）· 已落地（2026-08-09）· 范围扩展至全组件（2026-08-08）
> 状态：已落地（后端 Schema/路由/2FA/审计 + 前端 BFF/Hook/管理面板/导航改造均完成，并扩展为全组件注册表门控，ts-check/eslint 绿，服务层+审计集成自检通过）
> 范围：CS-Web-Backend + CS-Web-Frontend（monorepo 两个 submodule）
> 目标：root 管理员可在后台管理"功能模块是否对哪类用户展示"，并对每次修改执行**弹窗二次确认 + 2FA 验证**，复用现有代码逻辑，仅对源代码做最小必要改动。

---

### 0. 背景与现状（已通过代码探索核实）

| 维度 | 现状 | 结论 |
|---|---|---|
| 功能模块/菜单 | 前端**硬编码配置数组**（`navbar.tsx` 的 `NAV_LINKS`、各页面 `AdminTab`），后端**无** menu/nav 概念 | 可见性规则需新增后端数据源，前端改为从后端拉取 |
| 可见性过滤维度 | 前端仅两层：`requireAuth`（是否登录）+ `user.role`（admin/root）明文比较 | 需扩展为"未登录 / 已登录 / 管理员"三态规则 |
| 2FA | 后端 `TOTPService.verify(user_id, code)` 完整可用；前端 `useTwoFA` + BFF `/api/auth/2fa/verify` 完整 | **直接复用**，无需新写算法 |
| 二次确认弹窗 | 前端全局 `ConfirmProvider` + `useConfirm()`（danger/warning/info 变体）已就绪 | **直接复用** |
| 审计日志 | 后端 `POST /api/v1/audit/logs`（操作者身份服务端推导、防伪造）+ 前端 BFF `/api/admin/actions/*` | **直接复用**为修改留痕 |
| 站点设置存储 | 后端已有通用 `Setting` 模型（`module`+`key`→JSON `value`） | **直接复用**存储可见性配置 |
| 权限闸门 | BFF `requireRoot(req)` / 后端 `is_superuser` | 配置接口挂 root 守卫 |

**核心结论**：本功能所有"安全/交互/存储"底座均已存在，规划重点是**薄增量层 + 数据流打通**，不重写任何核心逻辑。

---

### 1. 数据模型设计（复用 `Setting`，零新表）

新增一条设置记录（存于现有 `settings` 表，无需 Alembic 迁移新表）：

```
module = "feature_visibility"
key    = "<功能模块标识，如 tools / community / events / about>"
value  = JSON: {
  "guest":     true/false,   // 未登录是否可见
  "member":    true/false,   // 已登录（普通用户）是否可见
  "admin":     true/false    // 管理员（admin/root）是否可见
}
```

- 全站所有受管模块的配置聚合在 `key` 维度；亦可改用单条 `key="all"` 存整个映射对象，二选一（推荐**按模块多行**，便于缓存与单点更新）。
- 读取走后端已存在的 `Setting` 仓储/服务（确认 `app/services` 是否有 `SettingService`；若无，新增一个轻量读取函数复用 `Setting` 模型与 `get_db`）。

---

### 2. 后端改动（CS-Web-Backend）

#### 2.1 新增 Schema — `app/schemas/feature_visibility.py`
- `VisibilityRule`（guest/member/admin 三布尔）
- `ModuleVisibility`（module_key + VisibilityRule）
- `FeatureVisibilityConfig`（list[ModuleVisibility]）—— 列表响应
- `UpdateVisibilityRequest`（module_key + VisibilityRule）

#### 2.2 新增路由 — `app/api/v1/admin/feature_visibility.py`
挂在 `/api/v1/admin` 下（与现有 admin 路由一致），全部依赖 `require_root`：

| 方法 | 路径 | 说明 | 守卫 |
|---|---|---|---|
| GET | `/admin/feature-visibility` | 拉取全部模块可见性 | `require_root`（is_superuser） |
| PUT | `/admin/feature-visibility/{module_key}` | 更新单模块可见性 | `require_root` + **2FA 校验** |

> 注意：PUT 接口内部**复用 `TOTPService.verify_or_raise(current_user.id, totp_code)`**（来自 `app/services/totp_service.py`）校验 2FA；请求体含 `totp_code` 字段。**强制 2FA（决策 B）**：落库前先检查 `current_user` 的 2FA 启用状态（读 `TwoFactorAuth.enabled`），未启用即抛 `TWO_FACTOR_NOT_SETUP` 拒绝修改，不允许"未启用直接放行"绕过。

#### 2.3 2FA 校验接入（复用）
在 PUT handler 中注入 `TOTPService`，于落库前调用 `await totp_service.verify_or_raise(current_user.id, body.totp_code)`。验证失败抛 `TOTP_INVALID`，前端弹错。

#### 2.4 审计留痕（复用）
更新成功后**调用 `AuditService.record`**（或 `POST /api/v1/audit/logs`）记录一条：
- `action = "feature_visibility.update"`
- `resource_type = "feature_module"`，`resource_id = module_key`
- `actor` 由服务端从 `current_user` 推导（不信任客户端）
- 详情含新旧规则

#### 2.5 注册路由
在 `app/api/v1/__init__.py` 或 `app/main.py` 的 admin 路由聚合处 `include` 新路由，保持与 `admin/users`、`admin/roles` 一致的前缀风格。

---

### 3. 前端改动（CS-Web-Frontend）

#### 3.1 BFF 路由 — `src/app/api/admin/feature-visibility/route.ts`（及 `[moduleKey]/route.ts`）
- 服务端 Route Handler，复用 `backend-client.ts` 转发到 `/api/v1/admin/feature-visibility*`
- 挂 `requireRoot(req)` 守卫（来自 `src/shared/security/guards.ts`）
- camelCase ↔ snake_case 翻译沿用现有 `backend-client` 逻辑

#### 3.2 可见性 Hook — `src/modules/admin/.../use-feature-visibility.ts`
- `fetchConfig()`：GET 拉取全部模块规则（SWR）
- `updateModule(key, rule, totpCode)`：PUT 提交（含 2FA 码）

#### 3.3 管理 UI — 新增 `src/modules/admin/ui/feature-visibility-panel.tsx`
在 `/admin` 页新增一个 Tab（与现有 `roles/users/logs` Tab 平级），展示模块列表 + 三态开关（guest/member/admin）。保存流程：
1. 用户点击"保存" → 调 `useConfirm()` 弹**二次确认弹窗**（复用 `confirm-dialog.tsx`）显示"将把 X 模块对 Y 类用户 显示/隐藏，此操作影响全站，确认？"
2. 确认后 → 弹出 **2FA 输入**（复用现有 `two-factor-form.tsx` / `useTwoFA` 的验证码输入 UI，仅取 `verify` 调用）
3. 2FA 通过 → 调 `updateModule` 提交 → 成功 Toast + `mutate` 刷新

#### 3.4 前端导航渲染改造（核心数据源切换）
改造 `src/components/layout/navbar.tsx` 与 `user-menu.tsx`（管理员入口）：
- 原 `NAV_LINKS` 的硬编码 `requireAuth` 改为从可见性配置计算：
  - `guest` 控制未登录可见；`member` 控制已登录可见；`admin` 控制管理员可见
  - 示例：`/tools` 的规则 `{guest:false, member:true, admin:true}` 等价于原 `requireAuth:true`
- 通过 SWR 拉取的 `FeatureVisibilityConfig` 做过滤；配置未加载时**降级为原硬编码行为**（保证可用性，fail-open 安全边界见 §5）
- `user-menu.tsx` 的 `/admin` 入口显隐也改为读 `feature_visibility` 中 `admin` 模块规则（不再纯靠 `role` 明文）

---

### 4. 受管模块清单（初版，含 admin 入口 · 决策 C）
初版纳入导航级模块：`about` / `events` / `community` / `tools` / `admin`（`admin` 入口本身可被隐藏，支撑"维护模式"场景）。后续可扩展至 `/profile`、`/join` 等。

---

### 5. 安全约束与边界
1. **root 专属**：配置读写均挂 `require_root`（后端 `is_superuser` + BFF `requireRoot` 双闸门）。
2. **2FA 强校验（决策 B）**：PUT 更新必须携带有效 TOTP，且 root **必须已启用 2FA**，否则拒绝（防"未启用直接放行"被绕过）。
3. **审计不可绕过**：所有变更经 `AuditService` 服务端落库，actor 身份服务端推导。
4. **前端降级策略**：可见性配置加载失败/网络异常时，前端**回退到原硬编码规则**（fail-open），避免整站导航消失；但真实权限闸门仍在 BFF/后端，前端隐藏不等于接口不可达（保持现有安全模型）。
5. **无新表迁移风险**：复用 `settings` 表，无需 Alembic 新迁移；但需在种子/初始化中保证默认可见性记录存在（否则首次读为空，触发降级）。

---

### 6. 落地步骤（建议顺序）
1. 后端 Schema + 路由 + 2FA/审计接入（§2），本地 `make dev-backend` 联调 Swagger。
2. 确认 `Setting` 读取服务/函数存在，补默认值种子。
3. 前端 BFF 路由 + Hook（§3.1–3.2）。
4. 前端管理面板 + 二次确认 + 2FA 输入（§3.3）。
5. 前端导航渲染改造（§3.4），SWR 拉取 + 降级逻辑。
6. 端到端验证：root 修改 → 二次确认弹窗 → 2FA 输入 → 全站导航即时变化 → 审计日志出现一条记录。
7. 更新 `openapi.baseline.json` 与 `docs/` 相关说明（若有接口契约基线）。

---

### 7. 决策点（已确认 · 2026-08-07）
- **A. 存储：按模块多行** ✅ —— `settings` 表每行一个 `module_key`，便于缓存与单点更新。
- **B. 2FA：强制要求** ✅ —— root 若未启用 2FA，PUT 更新接口直接拒绝（不允许"未启用直接放行"绕过）。
- **C. 受管范围：包含 `admin` 入口自身** ✅ —— 初版受管模块含 `about`/`events`/`community`/`tools`/`admin`（`admin` 入口可隐藏，支撑"维护模式"）。
- **D. 定时上下线：本期不做** ⏸ —— 留作后续迭代。

> 据此更新 §2.2 / §3.4 / §4 的约束：PUT 接口在 `verify_or_raise` 前显式检查 `current_user` 的 2FA 启用状态，未启用即抛 `TWO_FACTOR_NOT_SETUP`；§4 模块清单补入 `admin`。

---

### 8. 落地记录（2026-08-09）

#### 实现与草案的偏差（经现状校准）
1. **受管模块去掉 `community`**：community 已作为工具收纳进 `/tools`，不再是独立导航级模块。最终受管模块 = `about` / `events` / `tools` / `admin`，默认值与改造前 navbar/user-menu 硬编码行为逐一对齐。
2. **新增公开读取端点 `GET /feature-visibility`**：草案 §2.2 仅规划 root 专属 `GET /admin/feature-visibility`，但 navbar 需为**未登录访客**渲染导航，访客无法调用 root 端点。故拆出公开 GET（可见性规则本身不含敏感信息，真实权限闸门仍在 BFF/后端路由层，前端隐藏 ≠ 接口可达）。管理面板读取也复用此公开端点。
3. **路由文件扁平化**：后端无 `app/api/v1/admin/` 子目录，admin 路由为 `admin_*.py` 平铺。可见性读写集中在 `app/api/v1/feature_visibility.py`（GET 公开 + PUT root 专属，路径分别为 `/feature-visibility` 与 `/admin/feature-visibility/{module_key}`）。

#### 后端文件
- 新增 `app/schemas/feature_visibility.py`（VisibilityRule / ModuleVisibility / FeatureVisibilityConfig / UpdateVisibilityRequest，camelCase 传输）
- 新增 `app/services/feature_visibility_service.py`（复用 `Setting` 表 module=`feature_visibility`，DEFAULT_MODULES 默认值，upsert 只 flush）
- 新增 `app/api/v1/feature_visibility.py`（GET 公开；PUT = `_require_root` + `TOTPService.is_enabled` 强校验 + `verify_or_raise` + `AuditService.record_atomic` 原子提交）
- 改 `app/dependencies_services.py`（`get_feature_visibility_service` 工厂）、`app/api/v1/__init__.py`（注册路由）
- 无新表 / 无 Alembic 迁移（复用 `settings` 表）

#### 前端文件
- 新增 `src/shared/hooks/use-feature-visibility.ts`（SWR 共享缓存 + `useModuleVisible` + `DEFAULT_VISIBILITY` 降级）
- 新增 `src/app/api/feature-visibility/route.ts`（公开读取 BFF）
- 新增 `src/app/api/admin/feature-visibility/[moduleKey]/route.ts`（root 写入 BFF：requireRoot + Origin 白名单 + 限流）
- 新增 `src/modules/admin/ui/admin-feature-visibility-panel.tsx`（可见性矩阵 + 二次确认弹窗 + 2FA 6 位码输入）
- 改 `src/components/layout/navbar.tsx`、`src/components/user-menu.tsx`（读可见性配置 + 降级回退原硬编码）
- 改 `src/modules/admin/ui/types.ts`（AdminTab 加 `feature-visibility`）、`src/app/admin/page.tsx`（新 Tab，仅 root）
- 改 `src/i18n/messages/admin.ts`（`tabFeatureVisibility` / `tabTitleFeatureVisibility` + 新 `adminFeatureVisibility` namespace，三段式同步）

#### 验证
- 后端：`GET /api/v1/feature-visibility` 返回 4 模块默认配置（camelCase）；PUT 无鉴权 401；服务层 + 审计集成自检全通过（upsert / 持久化 / 审计计数 +1 / 恢复默认）。
- 前端：`tsc --noEmit` 0 错误；eslint 0 警告；BFF `/api/feature-visibility` 透传正常；`/admin`、`/tools` 页面 200。
- 2FA 强校验逻辑复用已验证的 `TOTPService`（`is_enabled` 前置 + `verify_or_raise`），审计复用 `admin_roles.py` 同款 `record_atomic` 模式。

### 9. 范围扩展：全组件可见性（2026-08-08 二次迭代）

#### 触发
用户纠正：初版只管理 4 个导航/控制台模块（"关于/活动/工具/管理"），但需求是**管理整个项目所有前端组件的开关**，而非仅控制台模块。故将受管范围从"导航级模块"扩展为"全站前端组件"。

#### 注册表（单一事实来源）
新增 `src/shared/feature-visibility/registry.ts`：枚举 **37 个受管组件**，分 5 组：
- `page`（9）：home / about / events / tools / community / profile / notifications / join / admin
- `chrome`（6）：navbar / announcement-banner / footer / theme-toggle / user-menu / language-switcher
- `workbench`（8）：wb-greeting / wb-today-tasks / wb-github-heatmap / wb-llm-usage / wb-quick-notes / wb-pomodoro / wb-exam-countdown / wb-assistant-chat
- `tools`（6）：tools-exam / tools-resource / tools-auxilio / tools-task / tools-dev-center / tools-admin-panel
- `community`（8）：community-feed / community-sidebar-nav / community-sidebar-trending / community-featured / community-search / community-tags / community-mine / community-admin

每个组件含 `key` / `group` / 中英文 `label`·`description` / 默认三态 `default`。**标签内联**，避免向巨型 i18n 注入 ~37×2 条专有名词；面板 UI 文案仍走 i18n。组件名与后端 `DEFAULT_MODULES` 的 key 一一对应。

#### 门控组件 + Hook 重构
- 新增 `src/shared/feature-visibility/visibility-gate.tsx`：`<VisibilityGate componentKey="...">`，对当前用户不可见时渲染 `fallback`（默认 null）；**未知 key 一律可见（fail-open）**，避免误隐藏。
- `use-feature-visibility.ts` 重构：`DEFAULT_VISIBILITY` 改为由 registry 派生；新增 `useComponentVisible(componentKey)`；保留 `useModuleVisible` 为兼容别名；`VisibilityRule` 类型定义移至 registry 并再导出。

#### 后端
`FeatureVisibilityService.DEFAULT_MODULES` 扩展为上述 37 组件（默认值与改造前硬编码行为对齐）。`get_config` / `get_rule` / `KNOWN_MODULE_KEYS` 均基于该 dict 自动覆盖，**无 schema / 路由改动**，PUT 校验自动支持全部 key。

#### 包裹点（渲染处门控）
- 框架：`layout.tsx` 包裹 Navbar / AnnouncementBanner / Footer；`navbar.tsx` 包裹 ThemeToggle / UserMenu，并新增 community 导航链接（已存在于 nav i18n）。
- 用户菜单：`user-menu.tsx` 包裹 profile 入口 / notifications 入口 / language-switcher。
- 页面级：`home` / `about` / `events` / `community` / `profile` / `notifications` / `join` / `admin` 各页成功态 `<main>` 包裹（loading/auth 屏不包裹，确保用户可见）。
- 工作台：`workbench.tsx` 注册表渲染循环按 widget `id` 包裹 8 个 widget；assistant 视图包裹 `wb-assistant-chat`。
- 工具：`tools/page.tsx` 6 个工具卡片按 `visibilityKey` 包裹 + `tools-admin-panel` 包裹。
- 社区：`community/page.tsx` 8 个子功能区块（feed / 侧栏导航 / 热榜 / 精选 / 搜索 / 标签 / 我的 / 管理）分别包裹。
- 管理面板：`admin-feature-visibility-panel.tsx` 改为按 5 组渲染全量矩阵（组标题 + 组件中英文名 + 三态开关 + 逐组件保存/2FA）。

#### 验证
- 前端：`tsc --noEmit` 0 错误；eslint 0 错误（既有未用变量 warning 非本次引入）。
- 后端：`py_compile` 通过；`DEFAULT_MODULES` 含 37 组件、key 与前端注册表一致。
- 沙箱限制：agent 无法常驻 dev server（进程在工具调用返回后被回收），实际浏览器验收需用户在自有终端运行 `zsh /tmp/wb-dev.sh both`。


